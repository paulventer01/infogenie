// Claims /manage and /manage/<view>. Panel rendering is handled by the legacy
// SPA via <SpaRouter/> in the dashboard layout; this route only owns the URL.
export default function ManagePage() {
  return null;
}
