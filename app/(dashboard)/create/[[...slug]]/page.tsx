// Claims /create and /create/<view>. Panel rendering is handled by the legacy
// SPA via <SpaRouter/> in the dashboard layout; this route only owns the URL.
export default function CreatePage() {
  return null;
}
