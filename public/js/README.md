# `public/js/` — per-feature frontend modules

This folder holds feature view-builders that were carved out of the monolithic
`app.js` (~50k lines). The goal: editing one feature only re-downloads that
feature's file, two people can touch different features without colliding, and
each file is small enough to read.

## The convention (read before adding a file here)

1. **Plain `<script>`, not an ES module.** The SPA injects views via
   `innerHTML` with inline `onclick=` handlers and calls builders by global
   name. So everything must stay a classic global script — no `import`/`export`,
   no bundler, no build step.

2. **Public entry points stay on `window`.** Each builder keeps its existing
   `window.buildX = function(){…}` (or `function buildX(){}` global) attachment,
   exactly as it was in `app.js`. That is what `navigateTo()`'s dispatch chain
   and inline `onclick="..."` handlers resolve against. Feature-private helpers
   stay inside the file's IIFE so they don't leak into the global namespace.

3. **One IIFE per file.** Wrap the feature in `(function(){ … })();` so its
   private helpers (`_esc`, `_f`, `_toast`, constants, …) are scoped to the file.
   These helpers are intentionally duplicated per file — that is what makes each
   file self-contained and safe to move.

4. **Shared globals come from `app.js`.** `showToast`, `navigateTo`, `_esc`,
   `_safeUrl`, `_escapeHtml`, `analysisData`, `_dataBadge`, Chart, etc. are
   defined in `app.js` and are available at *call time* (i.e. when the user
   navigates). Files here are loaded **after** `app.js` in `index.html`, but
   load order only matters for code that runs at load time — builders run on
   navigation, long after every script has executed.

5. **Per-file cache-busting.** Each `<script src>` in `index.html` carries its
   own `?v=YYYYMMDD<TAG>` string. When you edit a file here, bump **only that
   file's** `?v=` — you no longer have to bump (and force a re-download of) the
   whole `app.js`.

## How to extract another view from `app.js`

1. Find the feature's top-level IIFE (or its `build*` function plus its
   feature-private helpers). The existing `(function …(){ … })();` packs are the
   cleanest unit — they already attach only to `window`.
2. Move that block verbatim into a new `public/js/ig_<feature>.js` with the
   standard banner header. Do not change any code.
3. Leave a short pointer comment in `app.js` where the block used to be.
4. Add a `<script src="/public/js/ig_<feature>.js?v=…">` tag in `index.html`,
   after `app.js`, with its own cache-bust string.
5. Verify: load the app, navigate to the view, confirm it renders, charts draw,
   inline buttons fire, and there are no console errors.

## Files

| File | Views |
|---|---|
| `ig_studio.js` | Creator Studio (Brand Identity, Image Toolkit, AI Video, Presentations, Email Signature, Case Study, Page Insights, SEO Change Log) + WhatsApp/Voice/Bookings/Site/Link-in-Bio tiles |
| `ig_journey_omnichannel.js` | Customer Journey Builder · Real-time Signal Triggers · Omnichannel Composer |
| `ig_manage_pack.js` | New Project · Brand Calendar · Ask InfoGenie · Infographics · Heatmaps · Question Miner · AI Providers · Ad Swipe · Brand Foundation · Budget Board · Web Analytics |
| `ig_seo.js` | SEO On-Page Auditor (T22) · Multi-Page Crawler (T29) · GEO Audit (T30) · Local SEO · Social/OG Tags |
| `ig_onboarding.js` | Onboarding Wizard + Global Tour Button (novice-friendly UX layer) |
| `ig_true_roas.js` | True ROAS (T36) — Conversions · Revenue Lag · Settings (`_trState` + `buildTrueRoas` + `_tr*` helpers) |
| `ig_content_traffic.js` | Content Score (T39-A) · AI Traffic Monitor · Visibility Leaderboard |
| `ig_navchrome.js` | Navigation chrome — Slim Sidebar collapse/filter · auto-injected View Header + breadcrumbs · Related Views pill bar |
| `ig_reach_automation.js` | Reach automation cluster — Agency Hub (multi-client mgmt · white-label · scheduled sends) · Automations Center · Re-engagement Hub (incl. live Drip engine status panel + action helpers) |
| `ig_drip_serp.js` | Drip action helpers (`dripAction` · `generateSequenceContent` · `generateCounterOffer`) · Live Google SERP Intelligence (`initSerpView` · quick-search chips · `runSerpSearch` · `renderSerpResults`) · Brand Assets Library (`initBrandAssets` + `ba*` upload/preview/delete/remove-bg) |
| `ig_results_export.js` | Results view — PDF export (`exportResultsPDF`) · Competitor ROAS breakdown (`buildCompetitorROASBreakdown` + `getIndustryROASBenchmark`/`compROASIssues`) · Customise panel (`toggleResultsCustomisePanel` · `rcToggle` · `rcApply`) |
| `ig_csuite.js` | C-Suite Executive Intelligence Reports (`initCsuite` · `csSetRole`/`csSetPeriod` · `csData` · `csBuildView` · `csROIPanel` · `csBuildCEO`/`CMO`/`CFO`/`COO` · `csSuitePDF` · `csPeriodLabel`) · Action Center (`initActionCenter` · `acBuild`) |
| `ig_manual_comp_autoseo.js` | Manual Competitor Entry + Launch Now (`toggleManualCompPanel` · `addManualCompetitor` · `removeManualCompetitor` · `launchNow`) · Auto-SEO Pro (`buildAutoSEO` + calendar `showAutoSeoCalendar`/`publishNowFromCal`/`calEditArticle`/`calSaveEdit`/`calDeleteArticle` + keyword/backlink/outreach/settings/traffic renderers; `window._artDate` shared with Master Calendar) |
| `ig_mentions_gaps.js` | Mention Tracker + Real-time Alerts (`buildMentionTracker`/`runMentionTracker`/`renderMentionResults` · `loadAlertsList`/`toggleAlertsPanel`/`renderAlertsPanel`/`checkAlertsNow`/`markAllAlertsRead`/`checkCreditsNow`, `window._mentionsState`/`_alertsState`/`_mtSelectedComps`/`_mtSelectedCountries`) · Content Gaps (`buildContentGaps`/`runContentGaps`/`renderContentGaps`, `window._gapsState`/`_cgSelectedComps`) · Cross-Channel Report (`buildCrossChannel`/`runCrossChannel`/`renderCrossChannel`, `window._ccrState`) · Stakeholder digests (`buildStakeholders` + `add`/`remove`/`testStakeholderEmail`) · Webhook channels (`_loadWebhooks`/`_renderWebhooks` + `add`/`remove`/`toggle`/`testWebhook`) · Launch Calendar (`buildLaunches` + `add`/`removeLaunch`) · Company Intel (`showCompanyIntel`/`runCompanyIntel`) · `testSlackWebhook` · Ad-Platform Connections (`renderAdPlatformConnections`/`showAdPlatformSetup`) · AI Optimizer dashboard (`renderOptimizerDashboard` + `optimizer*`/`creativeRefresh*`/`bandit*`/`t34FatigueRunNow`). Reads shared `_esc`/`_escapeHtml`/`_safeUrl`/`showToast`/`currentCampaign` from `app.js` |
| `ig_content_social.js` | Content Calendar (`buildContent` — overview/clusters/gaps/page-audit/funnel tabs + helpers `runLighthouse`/`runRealPageAudit`/`generateCluster`/`suggestSeedTopic`/`openBuildContentModal`/`runBuildContent`/`bcCopy`/`bcDownload`) · Social Calendar (`buildSocialCalendar` + `openCreatePost`/`_buildSocialAnalytics`/`_buildSocialPublishing`/`socialToggleAutoPublish`/`_socialAutoPublishCheck`/`socialPublishNow`) · AI Visibility (`buildAiVisibility`). Reads shared `SOCIAL_PLATFORMS`/`FUNNEL_STAGES`/`POST_ARCHETYPES` consts + `window._social*`/`window._content*` state, all left in `app.js` |
| `ig_attribution_scorer.js` | Blended Performance (`buildBlendedPerf`/`loadBlendedPerf` + `_hydrateTrueRoasInline`/`_blendedHtml`) · Global Command Bar (`openCommandBar`/`closeCommandBar`/`prefillCommand`/`runCommandBar` + ⌘K keydown + spinner CSS) · Lead Qualifier (`refreshLeadQualifier`/`buildLeadQualifier`/`submitLeadQualification`/`deleteQualifiedLead`/`enrollHotLead` + `_leadQualifierHtml`/`_leadCard`) · Attribution & ROI (`buildAttribution`/`loadAttribution`/`_attributionHtml` + tabs `switchAttributionTab`/`loadAttributionCohorts`/`loadAttributionForecast`/`_cohortHtml`/`_forecastHtml`, `window._attrCurrentTab`) · Lookalike Audience Builder (`buildLookalike`/`generateLookalike`/`exportLookalikeCSV`/`_lookalikeResultHtml`, `window._lookalikeLastSpec`/`_laSelectSeed`) + `wireNewViews` nav dispatch · Content Optimisation Scorer (`buildContentScorer`/`runContentScorer`/`loadRelatedKeywords` + `_cs*` country-picker/keyword helpers, `window._csCountryList`/`_csSelectedCountries`). Wrapped in one IIFE; entry points re-attached to `window`. Reads shared `_escapeHtml`/`_esc`/`_safeUrl`/`showToast`/`navigateTo`/`_getDomainFromAnalysis`/`_getBrandFromAnalysis`/`_getCompetitorsFromAnalysis`/`_igMultiselect*` from `app.js` |
| `ig_intel_pack_b.js` | Standalone integration & intel view-builders (pack B), moved verbatim from `app.js` in one IIFE: Live LLM Scan / AI Search Visibility (`buildLlmScan` + `window._aivState`/`_aivRun`/`_aivRender`/`_aivLoadHistory`) · Post Performance (`buildPostPerformance`) · Email Broadcast (`buildEmailBroadcast`) · Hunter.io Email Finder (`buildHunter`) · LinkedIn Ad Spy (`buildLinkedinAds`) · Canva Template Launcher (`buildCanvaLauncher`) · CRM Sync (`buildCrmSync`) · Employee Advocacy (`buildEmployeeAdvocacy`) · Social Listening (`buildSocialListening`) · Media Intelligence (`buildMediaIntel`) · Hashtag Intelligence (`buildHashtagIntel`) · Maps Competitor Intelligence (`buildMapsIntel`) · Creative Intelligence (`buildCreativeIntel`) · YouTube Comment Miner (`buildYtCommentMiner` + `_ycmRender`/`_ycmLoadHistory`) · Link Prospector (`buildLinkProspector` + `_lpLastProspects`/`_lpFind`/`_lpApplyFilters`/`_lpRender`/`_lpLoadHistory`). Reads shared `showToast`/`navigateTo`/`_esc`/`_escapeHtml`/`_safeUrl`/`analysisData`/`_dataBadge`/Chart from `app.js` |
| `ig_research_tools.js` | Audience & keyword research view-builders, moved verbatim from `app.js` in one IIFE: ICP Studio (`buildICPStudio` + `_icp*` — auto-draft/VoC mining/activation flows incl. `_icpActCreativeTarget`/`_icpShowEmailSequenceModal`, `window._icpProfile`/`_icpVoC`) · Search Intent Map (`buildIntentMap`/`generateIntentMap` + `_im*` render/export/seed helpers, `window._intentMap`) · Keyword–Page Map (`buildKeywordMap`/`generateKeywordMap` + `_km*` page/competitor/keyword helpers incl. `_kmAddPage`/`_kmAddCompetitor`/`_kmRefreshKeywordsFromIntent`/`_kmExportCsv`/`_kmSendToBattlePlan`, `window._keywordMap`) · Amplitude AI Agents (`_AMP_AGENTS` + `buildAmplitudeAgents`/`runAmplitudeAgent`/`runAmplitudeReplayWithEvents` + `_amp*` render helpers). Entry points + inline-onclick helpers re-attached to `window`. Reads shared `analysisData`/`navigateTo`/`showToast`/`showTileInfo`/`hideTileInfo`/`generateCampaignRecs`/Chart from `app.js` |
| `ig_intel_pack_a.js` | Standalone integration & intel view-builders (pack A), moved verbatim from `app.js` in one IIFE: HubSpot Sync (`buildHubspotSync` + `_hs*`) · Meta Insights (`buildMetaInsights` + `_mi*`) · Keyword Explorer (`buildKeywordExplorer` + `_ke*`) · Google Ads Insights (`buildGoogleAdsInsights` + `_gai*`) · TikTok Ads Insights (`buildTiktokAdsInsights` + `_tti*`) · Social Publisher (`buildSocialPublisher` + `_sp*`) · Email Personalizer (`buildEmailPersonalizer` + `_ep*`) · YouTube Monitor (`buildYoutubeMonitor` + `_ym*`) · Weekly Report (`buildWeeklyReport` + `_wr*`) · Reddit Pulse (`buildRedditPulse`) · Ad Library (`buildAdLibrary` + `_al*`) · Newsletter Tracker (`buildNewsletterTracker` + `_nt*`) · Meeting Notes (`buildMeetingNotes`) · Headline Tester (`buildHeadlineTester` + `_n`/`_f`) · Review Aggregator (`buildReviewAggregator`) · Churn Scorer (`buildChurnScorer` + `_csRender`) · Twitter Pulse (`buildTwitterPulse`) · Job Board Spy (`buildJobBoardSpy`) · Video Script (`buildVideoScript`) · Chatbot Builder (`buildChatbotBuilder`) · Glassdoor (`buildGlassdoor`) · Quora Mining (`buildQuoraMining`) · Social Analytics (`buildSocialAnalytics`) · TikTok Downloader (`buildTiktokDownloader`) · Voiceover (`buildVoiceover`). Reads shared `showToast`/`navigateTo`/`_esc`/`_escapeHtml`/`_safeUrl`/`analysisData`/`_dataBadge`/Chart from `app.js` |
| `ig_plan_view.js` | Competitor ROAS Plan view (`buildPlanView`) — per-competitor 90-day execution roadmap shown on the `plan` view, moved verbatim from `app.js` in one IIFE. Entry point re-attached to `window`. The async entry wrapper `window.openCompPlan` stays in `app.js` and resolves `buildPlanView` by global name. Reads shared `analysisData`/`window._liveROAS`/`window._dataBadge`/`avg` from `app.js`; inline-onclick handlers resolve to shared Compete helpers (`openAdInCreativeStudio`/`openCampLaunchRich`) |
| `ig_core_views.js` | Core marketing view-builders, moved verbatim from `app.js` in one IIFE: Dashboard (`buildDashboard` + chart helpers `renderCTRChart`/`renderROASChart`/`renderTrendChart` + `window._loadDashboardDiag`) · Audience (`buildAudience`/`targetAudience`) · Creative (`buildCreative`/`buildCompetitorVsCards`/`generateCreatives`/`renderCreativeChart` + audience-panel/creative helpers `showAudiencePanel`/`showVsAudiencePanel`/`buildAudiencePanelHtml`/`initAudiencePanel`/`toggleSegment`/`activateTargeting`/`launchVsCampaign`/`launchCreativeCampaign`/`filterCreativeCards`/`regenCreatives`/`copyCreative`) · Reddit Intelligence (`buildRedditIntel`/`switchRedditTab`/`_rdtCard`/`rdtOpenReply` + helpers `scanRedditMonitor`/`generateRedditReply`/`suggestRedditPersona`/`suggestRedditTitle`/`autoFillRedditFields`/`_rdtKwSuggest` + `window._reddit*` state). Entry points + inline-onclick helpers re-attached to `window`. Reads shared `analysisData`/`showToast`/`navigateTo`/`_fmt`/`getCountryLabel`/`renderYourSwot`/`calcOpportunityScore`/the chart-instance vars/`openCompetitorAnalysis`/`openThreatModal`/`postReplyToReddit`/`_populateAdSpendColumn`/`_renderJourneyStages`/Chart from `app.js` |
| `ig_settings.js` | Settings / Integrations surface, moved verbatim from `app.js` in one IIFE: the `INTEGRATIONS` catalog (Ad Platforms · Intelligence · AI · CRM · Analytics · Communication · Productivity) · `buildSettings` (renders the tabbed integrations page) · `checkAPIHealth` (DataForSEO/RapidAPI status pill) · `buildIntegCard` (per-integration card) · `showIntegrationDoc`/`showDocsModal`/`closeDocsModal` (integration-docs modal + `switchSettingsTab` deep-links). Private helpers `_findIntegById`/`_getIntegApiDetails`/`_getCodeExample`/`_getTroubleshooting` stay file-scoped. Entry points (`INTEGRATIONS`, `buildSettings`, `checkAPIHealth`, `buildIntegCard`, `showIntegrationDoc`, `showDocsModal`, `closeDocsModal`) re-attached to `window` so `restoreConnectedStates`/`switchSettingsTab`/inline `onclick=` and `ig_content_pro.js`'s `window.buildSettings` monkeypatch resolve them. **Loads before `ig_content_pro.js`** (which reads `window.buildSettings` at load time). Reads shared `showToast`/`navigateTo`/`_esc`/`_safeUrl`/`analysisData` from `app.js` at call time |
| `ig_compete.js` | Compete cluster, moved verbatim from `app.js` in one IIFE: Competitors list + Competitor Monitor (`buildCompetitors`/`renderCompetitorCards`/`buildCompMonitor`/`buildCompCard` · Blog Monitor `generateBlogMonitor` · New Page Tracker `generatePageResponse`/`pageTrackerCounterAd` · backlinks `loadBacklinks`/`openBLDetail`/`closeBLDetail` + `_blRng`/`_blSeed`/`_BL_POOL`/`_genRefDomains`) · Intelligence Hub (`buildIntelligence` · `exportIntelligenceReport` PDF/TXT · `_loadJsPDF` · `openExclusiveModal`/`closeExclusiveModal`) · Battle Plan (`switchBattlePlanComp`/`buildBattlePlan` + `bp*` helpers `_bpGet`/`bpLC`/`bpGA`/`bpBC`/`bpCS`/`bpTA`/`bpCC`/`bpQW` · Full Attack Plan modal `openFullAttackPlanModal`/`renderAttackPlan`/`_apSwitchTab`/`_apCloseModal`). Entry points + all inline-onclick handlers re-attached to `window`. Reads shared `analysisData`/`showToast`/`navigateTo`/`_fmt`/`_esc`/`_escapeHtml`/`_safeUrl`/`_dataBadge`/`openCompetitorAnalysis`/`openAdInCreativeStudio`/`openCampLaunchRich`/`generateCampaignRecs`/Chart from `app.js` |
