# Brandwatch-Killer Suite — Tier Index (T1-T19)

Detailed per-feature notes. All tiers follow the same pattern: strict-JSON LLM prompt + `/^_DUMMY/i` key gate + template fallback + Postgres persistence + `_escapeHtml`/`_safeUrl` frontend builder. Full design history is in git.

## Tier 1 — Foundation (Search, Reports, Influencer CRM)

*   **T1 Search & AI Visibility** (Reach → Search & AI Visibility) · `POST /api/search-intel/{ai-visibility,pulse,images}/*` · multi-LLM (GPT-4o-mini + Claude Haiku + Perplexity + Gemini) brand-mention parser, DataForSEO Labs keyword pulse, GPT-4o-mini vision logo recognition · tables `search_intel_{llm_runs,pulse_runs,images}`.
*   **T1 One-Click Reports** (📊/📄/📈 on Search & AI Visibility) · `GET /api/exports/{pptx|pdf|xlsx}/{search-intel|campaigns}` · `services/exports/data_sources.js` declarative pipeline · pptxgenjs / exceljs / pdfkit.
*   **T1 Influencer CRM** (Reach → Influencer CRM) · `/api/influencers` REST + `POST /:id/draft-email` (GPT-4o-mini) · tables `influencers` + `influencer_outreach` · status pipeline prospect→contacted→negotiating→active.

## Tier 2 — Crisis, Battle Cards, Trends

*   **T2 Crisis Radar** (Monitor → Crisis Radar) · `/api/crisis-radar/{watchlist,incidents,snapshots,run-now}` · 6h cron, baseline = 7-snapshot moving avg, Slack alerts via SSRF-guarded webhook · tables `crisis_{watchlist,snapshots,incidents}`.
*   **T2 Battle Cards** (Compete → Battle Cards) · `POST /api/battle-cards/generate` (GPT-4o-mini, 4 strengths + 4 weaknesses + 3 moves + 4 counter-plays) · table `battle_cards`.
*   **T2 Trending Topics** (Monitor → Trending Topics) · `POST /api/trends/detect` (Perplexity sonar, 6-10 7-day topics) · table `trend_runs`.

## Tier 3 — Share of Voice, Influencer Discovery

*   **T3 Share of Voice** (Compete → Share of Voice) · `GET /api/sov/{series,targets}` · auto-populated by Crisis Radar 6h cron · table `sov_snapshots` · Chart.js stacked-area.
*   **T3 Influencer Discovery** (Reach → Influencer Discovery) · `POST /api/discovery/influencers` (Perplexity, ≥5k followers) · one-click "+ Add to Influencer CRM".

## Tier 4 — Daily Digest, Reply Assistant

*   **T4 AI Daily Digest** (Plan → Daily Digest) · `POST /api/digest/{run-now,/:id/send}` + 24h cron per watchlist brand (GPT-4o-mini, sections kind=warning|win|action|highlight) · table `digest_runs` · Slack send.
*   **T4 Reply Assistant** (Reach → Reply Assistant) · `GET /api/reply-assistant/inbox` + `POST /draft` (GPT-4o-mini, tone allowlist) · ranked top-30 mentions, long + 240ch X variant.

## Tier 5 — Press Releases, Alert Routing

*   **T5 Press Release Generator** (Plan → Press Releases) · `POST /api/press-release/generate` (GPT-4o-mini, kind=crisis_response|product_launch|milestone|counter_competitor|partnership|custom) · optional `from_incident_id`/`from_battle_card_id` hydration.
*   **T5 Smart Alert Routing** (Monitor → Alert Routing) · `/api/alert-routing` REST + `POST /test/:id` · trigger kinds `crisis_incident|sov_drop|digest_ready|mention_volume|custom` · channels Slack + email (Resend) · tables `alert_{rules,dispatches}`.

## Tier 6 — Backlinks, Content Calendar

*   **T6 Backlink Intel** (Reach → Backlink Intel) · `POST /api/backlinks/{summary,referring-domains}` (DataForSEO `/v3/backlinks/*/live`).
*   **T6 Content Calendar** (Plan → Content Calendar) · `POST /api/content-calendar/generate` (GPT-4o-mini, 1-30 days, channel allowlist 8) · table `content_calendar_runs` · CSV export.

## Tier 7 — Podcasts, A/B Designer

*   **T7 Podcast Monitor** (Monitor → Podcast Monitor) · `POST /api/podcast-monitor/scan` (Perplexity sonar, episodes with sentiment + platform) · table `podcast_monitor_runs`.
*   **T7 A/B Designer** (Plan → A/B Test Designer) · `POST /api/ab-designer/generate` (GPT-4o-mini, 8 element_kinds, 10 angles, length-aware) · table `ab_designer_runs`.

## Tier 8 — Voice of Customer, Pricing Watcher

*   **T8 Voice of Customer** (Monitor → Voice of Customer) · `POST /api/voc/mine` (GPT-4o-mini, 4-8 themes kind=praise|complaint|question|feature_request|neutral) · table `voc_runs`.
*   **T8 Pricing Watcher** (Compete → Pricing Watcher) · `/api/pricing-watch/{targets,scan/:id,snapshots/:id}` · Firecrawl `/v1/scrape` + GPT-4o-mini extract · tables `pricing_watch_{targets,snapshots}`.

## Tier 9 — Email Auditor, Landing Pages

*   **T9 Email Deliverability Auditor** (Reach → Email Auditor) · `POST /api/deliverability/audit` (pure DNS — MX + SPF + DKIM 19 selectors + DMARC + MTA-STS + BIMI) · weighted A-F grade.
*   **T9 Landing Page Builder** (Plan → Landing Page Builder) · `POST /api/landing-pages/generate` (GPT-4o-mini, hero + 4-6 features + 3-4 steps + 2-3 testimonials + 4-6 FAQs + final CTA) · `_renderHtml(content,{accent})` server-side responsive HTML · table `landing_pages` · sandboxed iframe preview.

## Tier 10 — Tech Stack, Cold Email

*   **T10 Tech Stack Detector** (Compete → Tech Stack Detector) · `POST /api/tech-stack/{detect,compare}` (BuiltWith Free API, normalised categories + live/dead pills, multi-domain matrix up to 5).
*   **T10 Cold Email Writer** (Reach → Cold Email Writer) · `POST /api/cold-email/generate` (GPT-4o-mini strict-JSON, 1-5 step sequence, tone allowlist, template fallback) · table `cold_email_runs`.

## Tier 11 — Web Vitals, Lead Finder

*   **T11 Web Vitals Auditor** (Compete → Web Vitals Auditor) · `POST /api/web-vitals/audit {url}` (Google PageSpeed Insights v5, mobile+desktop parallel, lab + CrUX field + top 6 opportunities).
*   **T11 B2B Lead Finder** (Reach → B2B Lead Finder) · `POST /api/lead-finder/search` (Perplexity sonar, never invents emails, max 2/company, `window._lfLeads` cache for HubSpot push) · table `lead_finder_runs`.

## Tier 12 — SERP Tracker, HubSpot Sync

*   **T12 SERP Position Tracker** (Compete → SERP Tracker) · `/api/serp-tracker/{keywords,scan/:id,scan-all,history/:id}` (DataForSEO `/v3/serp/google/organic/live/regular`, 15-country location_code map, exact-domain match) · tables `serp_tracker_{keywords,runs}`.
*   **T12 HubSpot Sync** (Reach → HubSpot Sync) · `/api/hubspot-sync/{test,push-lead,push-influencer,push-bulk,recent-contacts}` (`HUBSPOT_PRIVATE_APP_TOKEN` Bearer, batch upsert by email idProperty, scope-error hint).

## Tier 13 — Meta Ads, Keyword Explorer

*   **T13 Meta Ads Insights** (Optimize → Meta Ads Insights) · `/api/meta-insights/{test,account-summary,campaigns,top-ads}` (Graph API v19.0 `/act_{id}/insights`, allowlisted date presets, ROAS = revenue/spend, friendly `ads_read` hint).
*   **T13 Keyword Explorer** (Plan → Keyword Explorer) · `POST /api/keyword-explorer/explore` (DataForSEO Labs `keyword_overview` + `keyword_ideas` parallel, 15 countries, KD/CPC/intent, 5-50 ideas) · table `keyword_explorer_runs`.

## Tier 14 — Google Ads, TikTok Ads

*   **T14 Google Ads Insights** (Optimize → Google Ads Insights) · `/api/google-ads-insights/{test,account-summary,campaigns,top-ads}` (Google Ads API v17 GAQL `searchStream`, OAuth2 refresh-token flow with in-memory access-token cache, allowlisted date presets, friendly errors for dev-token / customer-id / OAuth client / refresh-token issues, optional `GOOGLE_ADS_LOGIN_CUSTOMER_ID` for MCC).
*   **T14 TikTok Ads Insights** (Optimize → TikTok Ads Insights) · `/api/tiktok-ads-insights/{test,account-summary,campaigns,top-ads}` (TikTok Marketing API v1.3 `/report/integrated/get/`, `Access-Token` header, date-preset → start/end YYYY-MM-DD mapping, `AUCTION_ADVERTISER|CAMPAIGN|AD` data levels, friendly scope/advertiser-id hints).

## Tier 15 — Social Publisher + Analytics

*   **T15 Social Publisher** (Reach → Social Publisher) · `/api/social-publisher/{test,profiles,accounts,connect-url,post,posts,schedule-calendar,analytics}` (Zernio API v1, `ZERNIO_API_KEY` Bearer, 15-platform allowlist twitter|instagram|facebook|linkedin|tiktok|youtube|pinterest|reddit|bluesky|threads|googlebusiness|telegram|snapchat|whatsapp|discord, OAuth-via-authUrl, post/schedule with mediaUrls + scheduledFor, bulk Content-Calendar scheduling, friendly 401/quota/profile/account hints).
*   **T15 Social Analytics** (Reach → Social Analytics) · `GET /api/social-publisher/analytics?profileId&range=7d|30d|90d` aggregates per-account engagement (posts/impressions/likes/comments/shares/clicks/engagement-rate/followers/follower-growth) + top post per account · strict per-account post matching (account-ID first, platform-fallback only when platform is unique on profile) · unified engagement extractor (analytics|engagement|stats keys) · tries dedicated `/accounts/:id/analytics` then `/analytics?accountId=` then derives from `/posts`.

## Tier 16 — Email Personalizer, YouTube, Weekly Report

*   **T16 Email Personalizer** (Reach → Email Personalizer) · `POST /api/email-personalizer/{personalize,bulk}` (Firecrawl `/v1/scrape` site research + GPT-4o-mini strict-JSON rewrite per lead, max 25 leads/batch, tokens [NAME]/[FIRST_NAME]/[COMPANY]/[ROLE]/[WEBSITE], one-click pull from `window._lfLeads`, CSV export, template fallback when AI/Firecrawl unavailable).
*   **T16 YouTube Monitor** (Monitor → YouTube Monitor) · `/api/youtube-monitor/{test,channels (GET/POST/DELETE),scan/:id,history/:id}` (Perplexity sonar pulls 3-15 recent videos per channel with views/likes/comments/sentiment/summary, no Google API key needed) · tables `yt_channels` + `yt_snapshots`.
*   **T16 Weekly Report** (Plan → Weekly Report) · `/api/weekly-report/{test,subs (GET/POST/DELETE),runs,preview,pdf,send}` · 7-day auto-cron emails PDF via Resend to all enabled subs · sections aggregate Crisis Radar incidents, SoV 7d-avg, mention volume, press releases, VoC themes, trends, optimizer decisions and YouTube activity from existing tier tables · graceful "no data" sections · uses existing `services/exports/pdf_report.js` `streamPdf()` · cron uses `FOR UPDATE SKIP LOCKED` claim-then-send for safe multi-process · tables `weekly_report_subs` + `weekly_report_runs`.

## Tier 17 — Reddit, Ad Library, Newsletter, Meeting Notes, Headlines

*   **T17 Reddit Pulse** (Monitor → Reddit Pulse) · `POST /api/reddit-pulse/scan` (Reddit public JSON `/r/{sub}/search.json` no-auth, 7-day window, up to 10 subreddits × 5 keywords, batch GPT-4o-mini sentiment classification) · table `reddit_pulse_runs`.
*   **T17 Ad Library Spy** (Compete → Ad Library Spy) · `POST /api/ad-library/{meta,tiktok}` (Meta Graph API v19.0 `/ads_archive` with `META_ACCESS_TOKEN` requiring ads_read scope, TikTok via Perplexity sonar scraping `library.tiktok.com/ads`, friendly scope/rate-limit hints).
*   **T17 Newsletter Tracker** (Compete → Newsletter Tracker) · `/api/newsletter-tracker/{targets (GET/POST/DELETE),scan/:id,history/:id}` (Firecrawl scrape archive URL + GPT-4o-mini extracts subject/sent_date/preview/url for last 10 issues, supports Substack/Beehiiv/Mailchimp) · tables `newsletter_targets` + `newsletter_issues`.
*   **T17 Meeting Notes** (Reach → Meeting Notes) · `POST /api/meeting-notes/summarize` (GPT-4o-mini strict-JSON, returns summary + key_points + action_items + BANT scores 0-10 each + overall_score 0-100 + deal_stage + sentiment + risks + objections + next_step, optional contact context).
*   **T17 Headline Tester** (Plan → Headline Tester) · `POST /api/headline-tester/test-headline` (GPT-4o-mini, scores original headline 0-100 + generates 3-10 variants across 10 kinds curiosity|negative|question|listicle|urgent|specific_number|contrarian|social_proof|how_to|outcome, each with patterns_hit + reasoning, sortable by score).

## Tier 18 — Reviews, Churn, Twitter, Job Boards, Video Scripts

*   **T18 Review Aggregator** (Compete → Review Aggregator) · `POST /api/review-aggregator/{scan,compare,runs}` (Perplexity sonar pulls reviews from Trustpilot/G2/Google/Capterra/TripAdvisor, returns avg_rating + sentiment-tagged reviews + total counts, compare mode 2-4 brands side-by-side) · table `review_aggregator_runs`.
*   **T18 Churn Scorer** (Reach → Churn Scorer) · `POST /api/churn-scorer/{score,bulk,scores}` (GPT-4o-mini scores each contact 0-100 churn risk with risk_level + signals + recommendation, bulk up to 25 contacts, UNIQUE on email upserts on conflict) · table `churn_scores`.
*   **T18 Twitter/X Pulse** (Monitor → Twitter Pulse) · `POST /api/twitter-pulse/scan` (Perplexity sonar searches X for brand+keywords last 7d, returns tweets with author/text/likes/retweets/replies/sentiment + viral flag for >1k likes OR >500 retweets) · table `twitter_pulse_runs`.
*   **T18 Job Board Spy** (Compete → Job Board Spy) · `POST /api/job-board-spy/scan` (Perplexity sonar pulls open roles from LinkedIn+Indeed+careers page, breaks down by department, generates 2-5 strategic signals about company direction from hiring patterns) · table `job_board_runs`.
*   **T18 Video Script Generator** (Plan → Video Scripts) · `POST /api/video-script/generate` (GPT-4o-mini, platforms tiktok|reels|shorts|linkedin, 6 tones, 15-180s, 1-5 variants per call, each with hook/body lines (spoken+onscreen+cue)/cta/viral_pattern/hashtags).

## Tier 19 — Chatbot, Glassdoor, Quora

*   **T19 Chatbot Builder** (Reach → Chatbot Builder) · `POST /api/chatbot/{generate,configs (GET),configs/:id}` (GPT-4o-mini, 5 tones friendly|professional|playful|concise|enthusiastic, returns greeting + 8-15 FAQ Q&A entries with kind tags + fallback + lead-capture fields + suggested quick replies + accent color, generates copy-pasteable embed snippet with copy-to-clipboard) · table `chatbot_configs`.
*   **T19 Glassdoor Sentiment** (Compete → Glassdoor Sentiment) · `POST /api/glassdoor/{scan,runs}` (Perplexity sonar pulls overall_rating + ceo_approval + recommend_pct + reviews with pros/cons/role/tenure/sentiment + top_complaints + top_praises + 2-5 culture_signals strategic insights) · table `glassdoor_runs`.
*   **T19 Quora Mining** (Reach → Quora Mining) · `POST /api/quora-mining/{mine,runs}` (Perplexity sonar finds 8-15 most-engaged questions with answer_count + view_count + intent classification informational|comparison|recommendation|complaint|how-to|definition + top_answer_summary + suggested_response_angle for the user's brand) · table `quora_runs`.

## Tier 20 — TikTok Downloader

*   **T20 TikTok Downloader** (Compete → TikTok Downloader) · `/api/tiktok-downloader/{test,parse,recent,proxy}` · accepts up to 25 TikTok URLs per request (tiktok.com / vm.tiktok.com / vt.tiktok.com — short links auto-resolve), resolves via tikwm.com public no-auth API in parallel, returns no-watermark mp4 URL + cover + caption + hashtags + music + author + views/likes/comments/shares/duration · `/proxy?url=` streams the mp4 through origin with strict allow-list (tikwm.com / tiktokcdn.com / tiktokv.com) so the browser gets a clean `Content-Disposition: attachment` save-as · table `tiktok_downloads`. Pairs with T17 Ad Library Spy — turn discovered competitor URLs into a local swipe file.

## Cross-cutting — Dynamic Audiences

*   **Dynamic Audiences (Reach → Dynamic Audiences)**: Drip-style real-time, rule-based contact segments. Phase 1 = builder UI + live preview. Phase 2 = 15-min sweep cron + HubSpot webhook (HMAC-validated when `HUBSPOT_WEBHOOK_SECRET` is set) + members drill-in. Phase 3 = bind any audience to a Drip email sequence — auto-enrol on join, auto-unsubscribe on leave (only enrollments tagged with the binding id are touched; manual enrollments untouched; mutations under `global._dripStore.lock`). Phase 4A = mirror membership to a HubSpot Static List (auto-creates the list via `POST /crm/v3/lists` on first save when `crm.lists.write` scope is granted; pushes joins/leaves via `/crm/v3/lists/{id}/memberships/add|remove`). Phase 4B = bind a churn-risk audience to a single-touch AI win-back: on join, fires a 1-step drip enrollment with the stored variant (regeneratable via `/api/reengage/generate`), tagged `audienceBindingId='reng:<bindingId>'` so onLeave can selectively auto-unsubscribe just the system-fired win-backs and never manual ones. All three bridges (drip, hs-list, re-engage) run after the membership write commits and are fanned out per-contact in parallel with per-target try/catch.

## Tier 21 — Voiceover

*   **T21 Voiceover** (Plan → Voiceover) · `/api/voiceover/{test,generate,list}` · POST `/generate` {text ≤4000 chars, voice ∈ alloy/echo/fable/onyx/nova/shimmer, model ∈ tts-1/tts-1-hd, label?} → calls `openai.audio.speech.create`, writes mp3 to `uploads/voiceovers/<ts>_<rand>.mp3`, returns `mp3Url` for direct browser playback + download · standard checks: `/^_DUMMY/i` key gate (returns 400 with friendly message), 4000-char limit, character-count display, mkdir-recursive on first call · table `voiceover_runs` (id, label, voice, model, char_count, mp3_url, created_at) · history widget shows last 10 with inline `<audio>` players. Pairs with T18 Video Script Generator — paste a generated script, pick a voice, get an mp3.

## Tier 22 — SEO On-Page Auditor

*   **T22 SEO On-Page Auditor** (Compete → SEO On-Page Auditor) · `/api/seo-auditor/{test,audit,runs}` · POST `/audit` {url} → SSRF-guarded fetch (10s timeout, 2MB cap, real-browser UA, follows up to 5 redirects), regex-parses HTML for 17 page-level checks + 2 root-level probes (robots.txt, sitemap.xml HEAD): https · title 30-60ch · meta-description 70-160ch · single H1 · viewport · html lang · canonical · OG (title/description/image) · twitter:card · JSON-LD · image alt-text % · noindex blocker · favicon · word count ≥600 · ≥3 internal links · no mixed content · H1 not empty · robots.txt · sitemap.xml. Each check is weighted (2-10 pts); pass = full pts, warn = ½, fail = 0. Total normalised to 0-100 with grade A (≥90) / B (≥80) / C (≥70) / D (≥60) / F. Frontend renders a colour-coded score banner + per-check cards sorted fail→warn→pass with the prioritised fix copy. Table `seo_audit_runs` (id, url, score, grade, checks JSONB, summary JSONB, created_at). `runAudit(url)` exported for reuse by T23. Pairs with T11 Web Vitals — speed + SEO twin tools.

## Tier 23 — Embeddable Audit Widget

*   **T23 Embeddable Audit Widget** (Compete → Embeddable Audit Widget) · `/api/seo-widget/{test,sites,sites/:id,sites/:id/leads,embed/:siteId.js,audit/:siteId}` · `POST /sites` {name, accent (#hex), ctaText, ownerEmail?} → returns `id` (`sw_<hex16>`) + paste-once snippet `<div id="infogenie-seo-widget"></div><script src=".../api/seo-widget/embed/sw_xxx.js" async></script>` · `GET /embed/:siteId.js` (public, CORS *, 5-min cache) returns IIFE that injects a styled inline form (URL + email + accent-coloured CTA button) into `#infogenie-seo-widget` (or appends to body) and POSTs to `/audit/:siteId` · `POST /audit/:siteId` {url, email?} runs `runAudit(url)` from T22, persists into `seo_audit_runs`, persists email→site mapping into `seo_widget_leads`, returns `{score, grade, teaser:{passed,warned,failed}, emailQueued, fullReport}` (full check details only when valid email present — gates lead capture) · `GET /sites/:id/leads` returns last 200 (email, url, score, grade, when) · CORS preflight (OPTIONS) handled · all input sanitised (`_safeColor`, `_safeText`, `_emailValid`). Frontend lists every widget with its embed snippet (one-click copy), live lead count, leads drill-in table, and delete (cascades leads). Built directly on T22 — pure lead-gen play for agencies running their own marketing site.

## Tier 24 — SEO Task Manager / Fix Tracker

*   **T24 SEO Task Manager** (Compete → SEO Task Manager) · `/api/seo-tasks/{test,list,stats,import-from-audit,reaudit,:id (PATCH/DELETE)}` · table `seo_tasks` (id, source='seo_audit', source_url, check_id, label, message, fix, priority 1|2|3, status open|in_progress|done|snoozed|wont_fix, assignee, due_date, snoozed_until, notes, audit_run_id, created_at, updated_at, closed_at) with idx on (status, priority, created_at) + (source_url, check_id). `POST /import-from-audit {auditRunId}` reads `seo_audit_runs.checks` JSONB, filters status∈warn|fail, creates one row per issue, dedupes against any existing task for same url+check_id whose status NOT IN done|wont_fix · priority derived from check (fail+weight≥6 → P1, fail or warn+weight≥6 → P2, else P3). `POST /reaudit {url}` re-runs `runAudit(url)` from T22, bulk-updates open tasks where the same check_id is now passing → `status='done', closed_at=now()` with auto-note appended; persists the new audit run; imports any newly-failing checks. `PATCH /:id` validates status enum, accepts priority/assignee/dueDate/snoozedUntil/notes, sets `closed_at` only when transitioning to done|wont_fix. SEO Auditor results page now shows a "📋 Add to Task Manager" button bound to the returned `runId`. Pairs with T22 — turns the audit from a snapshot into a living to-do list.

## Tier 25 — Schema.org / JSON-LD Generator

*   **T25 Schema.org / JSON-LD Generator** (Compete → Schema Generator) · `/api/schema-generator/{test,types,generate,save,list,:id (DELETE)}` · table `schema_blocks` (id, name, type, fields JSONB, jsonld TEXT, created_at) · 8 types served from a single `TYPES` manifest exposing field key/kind/required/placeholder/help/subFields → drives a fully dynamic frontend form. Supported: **Organization** (name*, url*, logo, sameAs[], phone, email, description) · **Article** (headline*, authorName*, datePublished*, dateModified, image, description, publisher, publisherLogo) · **Product** (name*, image, description, brand, sku, price+currency, availability, ratingValue+ratingCount → AggregateRating + Offer) · **FAQPage** (questions[]{question, answer} → mainEntity[Question/Answer]) · **LocalBusiness** (name*, address fields, geo lat/lng, priceRange, openingHours[]) · **BreadcrumbList** (items[]{name, url} → ListItem position) · **Event** (name*, startDate*, endDate, location+address, image, description, offer URL/price) · **Recipe** (name*, image, description, prep/cook time ISO 8601, recipeYield, ingredients[], instructions[] → HowToStep). `POST /generate {type, fields}` validates required fields, builds the @context/@type object, recursively strips empty values, returns `{jsonld: '<script type=\"application/ld+json\">...</script>', object}`. `POST /save` persists named blocks. Frontend has type-pill picker, dynamic per-type form with repeat groups (+/− rows), live JSON-LD preview, copy-to-clipboard, save/list/delete saved blocks. Pairs with T1 AI Visibility — proper schema is what ChatGPT/Perplexity/Gemini parse to cite your site.

## Tier 20 update — RapidAPI fallback

*   **T20 TikTok Downloader** now two-stage: stage 1 = tikwm.com (free, no auth), stage 2 = `tiktok-scraper7.p.rapidapi.com` via `TIKTOK_RAPIDAPI_KEY` env (paid ~$10/mo, much higher rate limits, survives WAF blocks). Stage 2 only attempted on stage 1 failure AND when key is set. Both adapters normalise to identical response shape via `_normalize()` so frontend is unchanged; `via` field reports which resolver served the result. `/test` endpoint surfaces both resolver states.

## Tier 26 — Unified Conversation Inbox

*   **T26 Unified Conversation Inbox** (Compete → Unified Inbox) · `/api/unified-inbox/{test, ingest, list, stats, :id (PATCH/DELETE)}` · table `unified_inbox_items` (id, source, source_id, source_url, author, title, content, sentiment, score, status new|replied|resolved|snoozed, assignee, tags JSONB, notes, raw JSONB, occurred_at, ingested_at, updated_at, handled_at) with `UNIQUE(source, source_id)` for race-safe dedup + indexes on (status,ingested_at), (source,ingested_at), (sentiment). `POST /ingest` scans the most-recent 20 runs of each conversation source — Reddit Pulse (T17), Twitter/X Pulse (T18), Review Aggregator (T18), Quora (T19), Glassdoor (T19), Newsletter mentions (T17), Chatbot conversations (T19) — normalises every payload via `_safeMap` (per-item try/catch so one bad row never aborts a batch) into `{source, source_id, source_url, author, title, content, sentiment, score, raw, occurred_at}` and bulk-upserts via `INSERT ... ON CONFLICT DO NOTHING`. `_sentiment()` coerces numeric (-1..1), short-string (`pos`/`neg`/`neu`) and rating-derived values; `_toDate()` accepts ISO strings, unix epoch seconds and Date objects, returning `null` for garbage. `GET /list` filters by source/status/sentiment/q with allowlist-validated enum filters and `ILIKE`-on-lower content search, all bound parameters (no string interpolation of user input). `PATCH /:id` validates the status enum, sets `handled_at` when transitioning to replied|resolved (and clears it on transition back to new), 404s on missing id. Frontend builder lives at the tail of `app.js` — filter bar (search + source + status + sentiment dropdowns) → status-coloured row list with sentiment dot, source badge, author, content excerpt, "open ↗", inline status select, 📝 Note, 🗑 Delete. Sets up the foundation for #2 (multi-user/role assignment per inbox item) without committing to that build yet. **Outcome:** the 7 monitoring tools that previously lived in separate dashboards now flow into one actionable stream — never lose track of who said what or whether you replied.

## Tier 27 — Conversion Boosters

*   **T27 Conversion Boosters** (Compete → 🚀 Conversion Boosters) · `services/conversion_boosters/{schema,api}.js` · two paste-once embeddable widgets sharing the same admin/storage layer:
    *   **Social Proof popup** — rotating "Sarah from Cape Town just signed up" toasts. Settings: `position` (4 corners, allowlisted), `intervalSeconds` (4-120, clamped), `displaySeconds` (2-30), `accent` (color regex), `items[]` (per-row Name/Location/Action/When, length-clamped, max 50).
    *   **Exit Intent popup** — desktop mouseleave-top + mobile rapid-scroll-up trigger. Modal with email input + CTA. Settings: `headline`, `subheadline`, `ctaText`, `accent`, `dismissCookieDays` (0-365), `redirectUrl` (URL-validated, http/https only).
*   **Tables**: `cb_widgets (id, type, name, settings JSONB, owner_email, created_at, updated_at)` + `cb_events (id, widget_id FK→cb_widgets ON DELETE CASCADE, event_type view|dismiss|lead, email, data JSONB, ip_hash, ua, created_at)` + indexes on `(widget_id, created_at DESC)` and `(widget_id, event_type)`.
*   **Routes**:
    *   Admin (auth-gated when `INFOGENIE_API_KEY` is set): `GET /test`, `GET /defaults`, `GET /widgets`, `POST /widgets`, `PATCH /widgets/:id`, `DELETE /widgets/:id`, `GET /widgets/:id/events`. PATCH/DELETE 404 on missing id. PATCH dynamically composes SET clause from a fixed allowlist of column names with all data parameterised — no SQLi.
    *   Public (allowlisted in `_AUTH_PUBLIC_API_PATHS`, CORS `*`, OPTIONS preflight handled): `GET /embed/:id.js` (5-min cache, IIFE renderer, no globals leaked, settings injected via `JSON.stringify` then DOM-built with `textContent` — no innerHTML on user data), `POST /event/:id` (view|dismiss ping, body capped to 4kb), `POST /lead/:id` (email validated by regex, captured into `cb_events`).
*   **Rate limiting**: `_rlKey()` uses `req.socket.remoteAddress` ALONE, never `req.ip`. server.js sets `trust proxy: true` which makes `req.ip` equal whatever XFF the client sends — fully attacker-controlled. Socket address is the actual TCP peer and unforgeable. Caps: leads 5/IP/5min + 60/widget/5min, events 30/IP/min + 600/widget/min. Verified: 8 sequential lead submits with rotating fake `X-Forwarded-For` headers → first 5 succeed, 6-8 hit 429.
*   **Inbox bridge**: every successful `/lead/:id` submit also `INSERT ... ON CONFLICT DO NOTHING` into `unified_inbox_items` with `source='social_proof'|'exit_intent'`, `source_id='cb-<widget>-<ts>-<rand>'`, so leads land in the T26 Unified Inbox stream automatically. Wrapped in try/catch in case the unified_inbox table is missing — non-fatal.
*   **Frontend builder** (tail of `app.js`): two type-pills (📣 Social Proof / 🚪 Exit Intent) → form with type-specific fields → list of existing widgets with view/lead counters, copy-to-clipboard embed snippet, edit/delete/activity-toggle. Activity panel shows view/lead/dismiss counters + last 200 events with type-coloured row stream. Cache buster bumped to `v=20260511AF`.
*   **Outcome**: paste one `<script src="…/api/conversion-boosters/embed/<id>.js" async></script>` tag onto any external marketing page → social proof toasts or exit-intent modal renders → captured emails flow into Unified Inbox + cb_events. Same architecture as T23, ready for follow-on work (campaign-tagged variants, A/B split, dynamic items pulled from `contacts` table).

---

## T28 — White-Label Reports

*   **Route**: `Reach › 🎨 White-Label Reports` (`view-white-label`, builder `buildWhiteLabel` at tail of `app.js`).
*   **Endpoints**: `GET /api/white-label/profile` · `PUT /api/white-label/profile` · `DELETE /api/white-label/profile` · `GET /api/white-label/preview/{pdf,pptx,xlsx}` (renders a sample report so the user can preview their theme without regenerating real data).
*   **Storage**: single global brand profile in `kv_store` under key `white_label.brand_profile`. No new schema. Shape: `{enabled, agencyName, logoDataUrl, primaryColor, accentColor, textColor, footerText, hideInfoGenieBranding}`. All inputs server-side normalised — colours regex-validated `#RRGGBB`, agency name capped at 80 chars, footer at 200, logo data URL must match `data:image/(png|jpeg|jpg|svg+xml);base64,...` and ≤350,000 chars (~256KB).
*   **Plumbing**: `services/exports/{pptx_report,pdf_report,xlsx_report}.js` each gained an optional final `brand` arg. Cover backgrounds, section/header bars, body text colour, footer line and (optionally) the cover logo are all parameterised. Defaults are unchanged InfoGenie navy/purple when `brand` is null. `services/exports/api.js` lazy-loads the white_label module, calls `getBrand()`, and passes the result through only when `brand.enabled` is true. Filename stem is also slugified from `agencyName` so exports become e.g. `acme-marketing-attack-plan-2026-05-11.pdf`.
*   **PDF logo caveat**: pdfkit can render PNG/JPG only — the PDF exporter rejects `data:image/svg+xml` logos silently. PPTX/XLSX accept any of the four formats.

## T29 — Multi-Page SEO Crawler

*   **Route**: `Reach › 🕸️ Site SEO Crawler` (`view-seo-crawler`, builder `buildSeoCrawler`).
*   **Endpoints**: `POST /api/seo-crawler/run {url, maxPages}` (returns `{id}` and starts BFS in background) · `GET /api/seo-crawler/runs` (list, 40 most recent) · `GET /api/seo-crawler/runs/:id` (full detail with `pages[]` + `live{progress, errors, status}` for in-flight runs) · `DELETE /api/seo-crawler/runs/:id`.
*   **Storage**: `seo_crawl_runs(id, root_url, max_pages, page_count, avg_score, site_grade, status, error, started_at, completed_at)` + `seo_crawl_pages(id BIGSERIAL, run_id REFERENCES, url, score, grade, summary JSONB, checks JSONB)` with index on `(run_id, score)`.
*   **Crawl loop**: 4 concurrent workers BFS-pop from a shared queue. Each iteration: SSRF re-validate → call `runAudit(url)` from T22 (which does its own fetch + 19-check audit) → INSERT row immediately so the user sees pages stream in via polling → if more pages still wanted, fetch HTML once more and `_extractLinks` (regex-based, same-host-only, dedupe via Set, drops asset extensions). Hard caps: `HARD_MAX_PAGES=100` · `HARD_TIMEOUT_MS=5min` · `CONCURRENCY=4` · queue buffer `(visited+queue) < maxPages*3`.
*   **Aggregation**: avg_score = mean of per-page scores, site_grade from same A-F bands as T22. Final status `completed` or `partial` (timeout/cap hit).
*   **In-memory `_runs` Map** holds live progress so the GET endpoint can return `live.progress` between DB writes — read-only side-channel keyed by unique `runId`, no cross-run contention.

## T30 — GEO Audit (Generative Engine Optimization)

*   **Route**: `Reach › 🤖 GEO Audit` (`view-geo-audit`, builder `buildGeoAudit`).
*   **Endpoints**: `POST /api/geo-audit/run {url}` · `GET /api/geo-audit/runs` (30 most recent) · `GET /api/geo-audit/runs/:id`. Module also exports `runGeoAudit(url)` for reuse.
*   **Storage**: `geo_audit_runs(id, url, score, grade, summary JSONB, checks JSONB, created_at)` with index on `(url, created_at DESC)`.
*   **12 weighted checks summing to 100**:
    1. **Question-style H2/H3 headings** (12 pts) — pass if ≥3 headings ending in `?`.
    2. **JSON-LD structured data** (12 pts) — pass if FAQPage present OR ≥2 blocks incl. Article/Organization.
    3. **Concise answer paragraphs** (10 pts) — pass if avg paragraph ≤80 words.
    4. **E-E-A-T author signals** (10 pts) — pass if author meta + bio block class found.
    5. **Freshness** (8 pts) — pass if `article:modified_time` or `<time datetime>` is <1y old.
    6. **Lead paragraph as direct answer** (10 pts) — pass if first `<p>` is 15-60 words and ends in `.!?`.
    7. **Lists & tables** (8 pts) — pass if ≥3 `<ul>/<ol>/<table>` blocks.
    8. **Image alt-text coverage** (6 pts) — pass if ≥90% of images have alt text (or zero images).
    9. **Internal links** (6 pts) — pass if ≥5 same-host links.
    10. **`/llms.txt` at site root** (8 pts) — HEAD probe, warn if missing.
    11. **Title 20-70 chars** (5 pts).
    12. **Meta description 70-160 chars** (5 pts).
*   **Pure regex, no LLM call** — predictable cost, deterministic output. Each check returns `{id, label, status, weight, earned, message, fix}` and the frontend sorts fail→warn→pass so the user sees the most-impactful fixes first. Same A-F grading bands as T22.

## T31 — Local SEO Basics
- **Route**: `/local-seo` (nav under Reach)
- **Endpoints**: `POST /api/local-seo/run`, `GET /api/local-seo/runs`, `GET /api/local-seo/runs/:id`
- **Storage**: `local_seo_runs` (id, url, score, grade, summary jsonb, checks jsonb, created_at)
- **Builder**: `buildLocalSeo` in app.js → `#lsWrap`
- **Service**: `services/local_seo/{schema,api}.js`
- **Checks (11, sum=100)**: phone (12) · click-to-call (8) · address (12) · LocalBusiness schema (15) · GBP/Maps link (10) · Maps embed (8) · hours (8) · contact page (8) · NAP consistency (6) · service area (8) · hygiene HTTPS+canonical (5)
- **Notes**: pure regex, no LLM. Recognises 15 LocalBusiness subtypes (Restaurant, Dentist, AutomotiveBusiness, etc).

## T32 — Social Tags Audit
- **Route**: `/social-tags` (nav under Reach)
- **Endpoints**: `POST /api/social-tags/run`, `GET /api/social-tags/runs`, `GET /api/social-tags/runs/:id`
- **Storage**: `social_tags_runs` (id, url, score, grade, summary jsonb, checks jsonb, created_at)
- **Builder**: `buildSocialTags` in app.js → `#stWrap`
- **Service**: `services/social_tags/{schema,api}.js`
- **Checks (13, sum=100)**: og:title (10) · og:description (8) · og:image (12) · og:type (4) · og:url (4) · twitter:card (8) · twitter:image+title (6) · Facebook Pixel (8) · GA4/GTM (8) · social profile links across 6 platforms (10) · favicon (6) · apple-touch-icon (6) · Organization sameAs[] (4)
- **Notes**: pure regex. UA (deprecated) detection issues warn; GA4/GTM passes. Profile detection excludes share-intent URLs (sharer.php, intent/share, p/, reel/).

## T33 — Headless Rendering + Bigger Check Inventory
- **Routes**: `POST /api/seo-auditor/audit` (now accepts `body.headless:true`) · `GET /api/seo-auditor/headless-status` · `POST /api/{geo-audit,local-seo,social-tags}/run` (all accept `body.headless:true`)
- **Service**: `services/_shared/headless_fetch.js` (shared puppeteer-core wrapper). Existing T22/T30/T31/T32 services updated.
- **Builder**: `buildSeoAuditor`, `buildGeoAudit`, `buildLocalSeo`, `buildSocialTags` — each gained a "Render with real browser" checkbox.
- **Browser**: puppeteer-core driving system Chromium found by probing `process.env.CHROMIUM_PATH`, `/usr/bin/chromium`, `which chromium`, then `/nix/store/*chromium*/bin/chromium`. Singleton instance with `--no-sandbox --disable-dev-shm-usage --single-process --no-zygote`. `--single-process` is what makes it survive Replit's container.
- **Request interception**: drops `image | media | font` to keep page weight low. Initial URL goes through `isUrlSafeToFetch`; cross-origin sub-resources are not individually validated (acceptable per inline comment — bodies of media types are dropped, only the rendered HTML of the requested page is collected).
- **Auto-fallback (T22 only)**: when raw HTML matches the empty-SPA heuristic (`looksLikeEmptySpa` — visible text < 400 chars AND markup contains a root container like `#root`/`#app`/`#__next`/`#__nuxt`/`#svelte`), T22 silently retries through headless and returns `renderMode: "headless-auto"`. Manual `headless:true` returns `renderMode: "headless"`. Default returns `renderMode: "http"`.
- **T22 expanded check inventory (#20-32, 13 new)**: doctype html5 (2) · UTF-8 charset (2) · HSTS response header (4) · X-Robots-Tag header (3) · hreflang (2) · web manifest / PWA (2) · theme-color meta (2) · RSS/Atom feed (2) · inline event handlers (2) · loading="lazy" image % (2) · heading hierarchy with no skipped levels (3) · duplicate `<meta name="description">` (2) · inline `<script>` byte size (2). Total weight grew from ~85 → ~116; final score is still `(earned/totalWeight)*100` so % stays correctly normalised.
- **Notes**: response headers now flow from `_fetchHtml` (and from puppeteer's `resp.headers()`) into `_parseAndScore(html, finalUrl, headers)` so HSTS and X-Robots-Tag can be checked. T30/T31/T32 INSERT statements now `JSON.stringify` summary/checks for JSONB columns (matched T22's pattern; previous bug submitted raw objects on T30).

## T34 — Madgicx-style Optimizer Upgrades

Five Madgicx-inspired enhancements stacked on the existing AI Optimizer. All are recommendation-only — they never auto-pause campaigns or change budgets without human approval.

### Feature 1 — Blended Summary hero card
- **Where**: Cross-Channel Report → top of the page (replaces the old Total Spend / Customers / CAC / ROAS hero).
- **Renders**: 4-card strip — Total Ad Spend · MER · LTV/CAC · Net Sales — with a platform-pill row above (live/dim per connection) and a secondary line for Customers / Total Revenue / Blended ROAS.

### Feature 2 — MER metric (+ per-channel revenue/ROAS/CPM)
- **Endpoint**: `GET /api/blended-roas`
- **Helper**: `_revenueByPlatform(days)` queries `ad_performance_hourly JOIN ad_campaigns` summed by platform.
- **New fields**: `totalRevenue`, `mer` (% — revenue/spend × 100), `netSales` (revenue − spend), `ltvCac`, `ltvAssumed` (default 2× CAC until real LTV wired). Per-channel objects also gain `revenue`, `roas`, `cpm`, `revenueSource`.

### Feature 3 — Day-part / hour-of-day budget shifting
- **Service**: `services/optimizer/dayparting.js`
- **Storage**: `optimizer_dayparting` table (campaign_id · window_days · total_spend/conv · hours_json[24] · best_hours[4] · worst_hours[4] · recommendation · run_id).
- **Logic**: 14-day rolling aggregation by `EXTRACT(HOUR FROM bucket_hour AT TIME ZONE 'UTC')`; scores each hour by ROAS → falls back to inverse CPA → falls back to CTR; skips campaigns with <$25 total spend or <6 hour buckets with data.
- **Cron**: `startDaypartingCron(24)` — first run 3 min after boot, then every 24h. Mutex-guarded.
- **Routes**: `GET /api/optimizer/dayparting` (latest per campaign) · `POST /api/optimizer/dayparting/run-now`.

### Feature 4 — "Why did the AI do this?" Decision Log
- **Route**: `GET /api/optimizer/decisions?limit=&platform=` joins `optimizer_actions`+`ad_campaigns` so each row carries campaign name, platform, before/after JSON, applied/error state, run_id.
- **UI**: Grouped count pills (pause/scale_budget/hold/creative_refresh/bandit) + scrollable list with colour-coded left border, before→after JSON diff, plain-English reason, run id, timestamp.

### Feature 5 — Predictive creative fatigue
- **Service**: `services/optimizer/fatigue_forecast.js`
- **Storage**: `creative_fatigue_forecasts` table (campaign_id · creative_id · platform_ad_id · window_days · samples · current_ctr · slope_per_day · projected_ctr_3d · days_until_floor · ctr_floor · predicted_fatigue · reason · run_id).
- **Logic**: OLS linear regression on daily CTR over 14d (skips days with <50 impressions, requires ≥5 usable days). Flags `predicted_fatigue=true` when slope<0 AND projected CTR at +3d < 0.005 (0.5% floor — same as `creative_refresh.js`). Computes `days_until_floor` from line crossing.
- **Cron**: `startFatigueForecastCron(24)` — first run 4 min after boot, then every 24h. Mutex-guarded.
- **Routes**: `GET /api/optimizer/fatigue-forecast?flagged=1` (latest per creative) · `POST /api/optimizer/fatigue-forecast/run-now`.
- **Pairs with**: existing 72h Creative Auto-Refresh — predictive fatigue is the early-warning layer that catches creatives BEFORE they hit the existing reactive refresh threshold.

## T35 — SEOptimer-inspired upgrades

Three upgrades layered on existing T22 (SEO Auditor) and T6 (Backlinks).

### Feature 1 — Bulk Reporting
- **Service**: `services/bulk_reporting/{schema,api}.js`
- **Storage**: 2 tables — runs + per-URL items.
- **Flow**: multer CSV upload → 500-URL cap → DB-backed queue resumable across restarts → 4-parallel worker reuses T22 `runAudit` per URL → branded PDFs via `archiver` zip stream (reuses T28 brand profile + `streamPdf`-to-buffer adapter).
- **Routes**: `POST /api/bulk-reports/run` · `GET /api/bulk-reports/:id/{zip,csv}` (csv = flat per-check rows).

### Feature 2 — Backlink Change Monitor
- **Service**: `services/backlink_monitor/{schema,api}.js`
- **Storage**: 3 tables — monitors / snapshots / changes.
- **Logic**: daily/weekly per-domain DataForSEO `referring_domains/live` snapshot diffed against prior snapshot. **Persists `new`/`lost` change rows BEFORE mutating snapshots** so a crash mid-write doesn't lose the diff. Optional Slack webhook + Resend email digest per monitor. Hourly cron with per-monitor frequency gate.

### Feature 3 — Backlink Research dimensions
- Extends existing T6 `services/backlinks/api.js` with `/anchors`, `/pages`, `/breakdown`. Anchors+pages call DataForSEO live; breakdown derives TLD+Country aggregations locally from a single `referring_domains` pull to save credits. Surfaced via "🔬 Deep Research" button on Backlink Intel view.

## T36 — True ROAS

Closes the offline-revenue blind spot. Recommendation-only — never auto-applies budget changes.

- **Service**: `services/true_roas/{schema,api}.js`
- **Storage**: `offline_conversions` table — UNIQUE(source, source_deal_id), indexes on closed_at / platform / lower(email). All currency stored as `revenue_cents` BIGINT; mixed-currency NOT auto-converted (UI flags).
- **CSV uploader**: lenient header detection (deal_id / email / revenue / currency / fbclid / gclid / ttclid / platform / closed_at / lead_created_at), multer 5MB cap, ON CONFLICT upsert.
- **HubSpot sync**: paginates `/crm/v3/objects/deals` (10 pages × 100), filters by `dealstage` ∈ configured stages, attribution from fbclid/gclid/ttclid → meta/google/tiktok else `unknown`. 6h cron when `hubspotSyncEnabled`.
- **`computeTrueRoas(days)`**: joins `ad_performance_hourly`+`offline_conversions` per platform → returns `{trueRoas, reportedRoas, online, offline, spend, perPlatform:{uplift%}}`.
- **Cross-Channel hero patch**: adds inline 💰 `True ROAS:` line to `_blendedHtml` Blended Summary via `_hydrateTrueRoasInline` (fetches after innerHTML swap) showing uplift % vs reported.
- **Profit-aware budget recommendations**: apportions per-platform offline revenue across each campaign by its share of online revenue, computes 7d True ROAS per campaign, inserts `optimizer_actions` rows with `action='budget_cap_cut'`/`'budget_cap_scale'` (mode='recommend', status='pending', 24h dup gate) when below `minTrueRoasThreshold` (-25%) or above `scaleTrueRoasThreshold` (+20%).
- **Revenue lag report**: weekly cohorts on `lead_created_at` showing avg days-to-close + total revenue per cohort.
- **UI**: `/#true-roas` with 3 tabs (Conversions / Revenue Lag / Settings).
- **Routes**: `/settings`, `/upload`, `/sync-hubspot`, `/conversions`, `/summary`, `/revenue-lag`, `/budget-recommendations/run`.

## T37 — Get-Started Roadmap

- **Service**: `services/roadmap/{schema,catalog,api}.js`
- **Storage**: `roadmap_progress` table keyed by stable `task_id`.
- **Catalog**: 90 day-by-day tasks split SEO / SMM / PPC × 30 days each. Every task deep-links to the InfoGenie view that automates it via `navigateTo(view)`. Plus weekly social cadence cards (Instagram / Facebook / LinkedIn) from the lead-magnet schedule + the 5 PLAN/CREATE/ENGAGE/ANALYZE/GROW principles.
- **UI**: progress bar in hero, filter pills SEO/SMM/PPC/All, ↻ Reset button. Nav link "🗺️ Get Started Roadmap" sits directly under Dashboard so beginners find it first.
- **Routes**: `GET /catalog`, `GET /progress`, `POST /progress/:taskId`, `DELETE /progress/:taskId`, `POST /reset`.

## T38 — Viral Carousel Generator

- **Service**: `services/carousel/{schema,api}.js`
- **Storage**: `carousels` table.
- **Structures**: 4 STRUCTURES = pure-info / storytelling / problem-solution / listicle. Each has a 10-slot 10-slide template following the **Hook → Context → Value → Action** framework.
- **AI**: OpenAI gpt-5-mini strict-JSON with `/^_DUMMY/i` key gate + template fallback.
- **Routes**: `/structures`, `/generate`, `/list`, `/:id`, `DELETE /:id`.
- **UI**: `/#carousel` under Create nav with structure picker, brand-voice/audience inputs, 10 colour-graded slide cards, "📋 Copy all" + "📤 Open Social Publisher" + "📅 Add to Calendar" handoffs, recent-history list.
- **Pairs with**: Reddit Pulse "💡 Discover Questions Customers Ask" panel which calls `POST /api/reddit-pulse/discover-questions` (pure Reddit search across 4 query variants `?` / `how to` / `best` / `help`, question-title filter, scored by upvotes+comments×2). "→ Make Carousel" button pre-fills the topic field.

## T39-A — Content Score Auto-Optimize

- **Service**: `services/content_score/{schema,api}.js`
- **Storage**: `content_score_runs` + `content_score_fixes` tables.
- **Scoring**: 8 subscore dimensions — keyword density, meta tags, content depth, heading structure, featured-snippet eligibility, schema markup, internal/external links, image alt coverage. Each 0–100; weighted average = overall score.
- **AI Auto-Fix**: `POST /api/content-score/fix` sends failing subscore context to GPT-4o-mini → returns JSON `{subscore, issue, fix_text, priority}` for each. `POST /api/content-score/auto-optimize` bundles all fixes in one prompt.
- **Routes**: `POST /score`, `POST /fix`, `POST /auto-optimize`, `GET /runs`.
- **UI**: `/#content-score` under Reach → "Get found in search & AI" nav group. URL input → score card with 8 gauge bars → "🤖 Auto-Optimize All" button → fix panel with copy buttons per subscore.

## T39-B — AI Traffic Monitor

- **Service**: `services/ai_traffic/{schema,api}.js`
- **Storage**: `ai_traffic_sites` + `ai_traffic_events` tables.
- **Detection**: 15 AI referrer domains (chatgpt.com, perplexity.ai, claude.ai, gemini.google.com, copilot.microsoft.com, you.com, phind.com, etc.). `POST /api/ai-traffic/beacon` ingests referrer+path from the 1-line pixel; normalises to source label.
- **Routes**: `/pixel.js` (embed snippet), `/beacon`, `/stats`, `/sites` CRUD.
- **UI**: `/#ai-traffic` under Manage → "1 · Plan the work" nav group. Site list → stats cards (total hits, top source, 30d trend) → chart of AI referrals by source → pixel embed code copy button.

## T39-C — Visibility Rank Table with Deltas

- **Extension of T26** (`services/ai_visibility/api.js`).
- **New route**: `GET /api/ai-visibility/leaderboard` — reads last 2 SOV runs from `ai_visibility_results`, ranks competitors by latest SOV score, computes Δ vs previous run, returns `{rank, name, sov, delta, trend}` array.
- **UI**: `/#vis-leaderboard` under Grow nav group. Sortable table with rank badge, brand name, SOV % bar, Δ chip (▲green / ▼red / —), trend sparkline. Refreshes on load.

## T39-D — Reddit Reply Generator (Tone Presets)

- **Extension of T29** (`services/reddit_pulse/api.js`).
- **New route**: `POST /api/reddit-pulse/generate-reply` — accepts `{postTitle, postBody, tone: engaging|direct|balanced}` → GPT-4o-mini generates a contextual, non-spammy Reddit reply matching the tone preset.
- **UI enhancement**: Each post card in Reddit Live Pulse gains tone pills (Engaging / Direct / Balanced) + "✍️ Generate Reply" button → reply modal with copy button. Tone state persisted in `window._rpTones`.


## T40 — Hashtag Intelligence (Instagram & TikTok)

- **Service**: `services/hashtag_intel/{schema,api}.js`
- **Storage**: `hashtag_intel_runs` table (tenant_id NOT NULL, platform, seed_keyword, hashtags JSONB, clusters JSONB, total_count).
- **Data source**: Perplexity Sonar (`POST /chat/completions` with `model: sonar`) for live hashtag research; GPT-4o-mini for cluster analysis and strategy generation.
- **Routes**: `POST /api/hashtag-intel/research` (Perplexity sonar research → GPT cluster), `GET /api/hashtag-intel/runs` (history, optional `?platform=`), `GET /api/hashtag-intel/runs/:id`, `DELETE /api/hashtag-intel/runs/:id`.
- **Cluster structure**: Each cluster has `name`, `description`, `hashtags[]`, `reach` (Mega/High/Medium/Niche), `strategy`, `post_frequency`.
- **Template fallback**: When Perplexity key is absent, returns deterministic template tags and clusters based on the keyword — clearly labelled as template.
- **UI**: `/#hashtag-intel` under Reach → "4 · Publish organically" nav group. Platform picker (Instagram/TikTok), keyword input, strategic cluster cards with copy-all per cluster, full sortable hashtag table with niche score bars, caption-ready copy block (top 30 tags), run history with load/delete.

## Tier 41 — Google Maps Competitor Intelligence

*   **T41 Maps Intel** (Compete → 🗺️ Maps Intel) · `services/maps_intel/{schema,api}.js` · `POST /api/maps-intel/scan {keyword, region}` — Perplexity Sonar researches 12-20 local competitor businesses from Google Maps data, GPT-4o-mini generates a `market_summary` (density, avg_rating_benchmark, market_maturity, opportunities[], threats[]). `GET /api/maps-intel/runs` (last 20, tenant-scoped). `DELETE /api/maps-intel/runs/:id`. Table `maps_intel_runs` (id, tenant_id NOT NULL, keyword, region, businesses JSONB, market_summary JSONB, total_count, created_at).
- **Data source**: Perplexity Sonar (`model: sonar`) for live Google Maps business discovery; GPT-4o-mini strict-JSON for market summary analysis.
- **Routes**: `POST /api/maps-intel/scan` (Perplexity research → GPT summary), `GET /api/maps-intel/runs` (history), `GET /api/maps-intel/runs/:id`, `DELETE /api/maps-intel/runs/:id`.
- **Business fields**: name, address, rating (1.0-5.0), review_count, price_range ($|$$|$$$|$$$$), website, competitive_signal.
- **Template fallback**: 12 deterministic businesses generated from keyword/region when Perplexity key absent — `source:'template'` honesty tag.
- **UI**: `/#maps-intel` under Compete → "3 · Spy on what they're doing". Keyword + region inputs with 🧠 AI Suggest buttons (two separate suggesters), Scan Market button, market summary card (avg rating, density, market maturity badge, opportunities/threats), competitor table sortable by name/rating/review_count, filter dropdowns (min rating, min reviews), run history with load/delete.

## Tier 42 — Creative Intelligence (What Makes Content Perform)

*   **T42 Creative Intel** (Create → 🎬 Creative Intel) · `services/creative_intel/{schema,api}.js` · `POST /api/creative-intel/analyze {urls[]}` — accepts up to 20 TikTok, YouTube Shorts, or Instagram Reels URLs; calls Perplexity Sonar in one batched prompt to fetch title/caption/hashtags/views/likes/comments/duration/platform per video; then one GPT-4o-mini strict-JSON call analyses all videos for hook_type/has_text_overlay/emotion_trigger/cta_present/music_type/length_bucket and synthesises `patterns` (hook_styles, emotion_triggers, music_types, length_buckets, text_overlay_pct, cta_pct, avg_duration_sec, avg_engagement_rate, optimal_length_range, winning_formula[]) + `creative_brief` (full actionable brief). `GET /api/creative-intel/runs` (last 20, tenant-scoped). `GET /api/creative-intel/runs/:id`. `DELETE /api/creative-intel/runs/:id`. Table `creative_intel_runs` (id, tenant_id NOT NULL, urls JSONB, analyses JSONB, patterns JSONB, brief TEXT, url_count, created_at).
- **Data source**: Perplexity Sonar (`model: sonar`) for live video metadata; GPT-4o-mini strict-JSON for creative analysis + patterns + brief.
- **Template fallback**: Deterministic 10-video analyses generated from url/platform detection when Perplexity key absent — `source:'template'` honesty tag.
- **Platform detection**: TikTok (tiktok.com), YouTube Shorts (youtube.com/shorts), Instagram Reels (instagram.com/reel|/p/) — all other URLs classified as Unknown.
- **Hook types**: curiosity_gap | bold_claim | question | listicle | controversy | story | how_to | social_proof.
- **UI**: `/#creative-intel` under Create → "3 · Design the visuals". URL textarea (one per line), Analyse button, per-video card grid (platform badge, metrics, hook/emotion/CTA/music tags, caption excerpt), pattern analysis panel with percentage bars (hook styles / emotion triggers / music types / length buckets), winning formula bullets, full creative brief panel with "→ Open Video Script Generator" handoff button (pre-fills topic field in video-script view).

## Tier 43 — YouTube Comment Mining for Content Ideas

*   **T43 YouTube Comment Miner** (Compete → 💬 Comment Miner) · `services/yt_comment_miner/{schema,api}.js` · `POST /api/yt-comment-miner/mine {channelId?, videoUrl?}` — accepts either a tracked channel ID (from YouTube Monitor) or a direct YouTube video URL; calls Perplexity Sonar to collect 80-120 real audience comments from the channel's latest videos; then calls GPT-4o-mini strict-JSON to classify comments into questions/themes/sentiment and generate 6-10 actionable content ideas. `GET /api/yt-comment-miner/runs` (last 20, tenant-scoped). `GET /api/yt-comment-miner/runs/:id`. `DELETE /api/yt-comment-miner/runs/:id`. Table `yt_comment_runs` (id, tenant_id NOT NULL, channel_id, channel_name, video_url, questions JSONB, themes JSONB, content_ideas JSONB, sentiment_breakdown JSONB, comment_count, created_at).
- **Data source**: Perplexity Sonar (`model: sonar`) for live YouTube comment collection; GPT-4o-mini strict-JSON for classification + content idea generation.
- **Template fallback**: 6 deterministic questions, 5 themes, 6 content ideas, and sentiment breakdown when Perplexity or OpenAI key absent — `source:'template'` honesty tag.
- **Comment fields**: text (up to 200 chars), likes, video title/url.
- **Questions**: 5-10 recurring questions sorted by estimated recurrence count.
- **Themes**: 4-6 dominant content-gap themes with sentiment label (positive/neutral/negative) and comment count.
- **Content ideas**: 6-10 ideas each with title (ready-to-use), angle (specific approach), and why_it_works (evidence from comments).
- **Sentiment breakdown**: positive + neutral + negative percentages summing to 100.
- **UI**: `/#yt-comment-miner` under Compete → "6 · Listen for mentions" nav group. Channel selector dropdown (fetched from `/api/youtube-monitor/channels`) OR paste a YouTube video URL. Mine button. Results: Questions panel + Themes panel side-by-side, Sentiment breakdown bar, Content Ideas cards each with "→ Add to Calendar" button (POSTs to `/api/content-calendar/add`). Run history with delete. YouTube Monitor channel cards gain a "💬 Mine" shortcut button (navigates to Comment Miner with that channel pre-selected).

## Tier 44 — Automated Link Prospecting

*   **T44 Link Prospector** (Reach → 🎯 Link Prospector) · `services/link_prospector/{schema,api}.js` · `POST /api/link-prospector/find {keyword, domain}` — calls DataForSEO SERP live (`/v3/serp/google/organic/live/regular`) to get top 20 organic pages ranking for the keyword; then calls Perplexity Sonar in one batched prompt to enrich each page with domain_strength (high/medium/low), content_relevance (0-100), links_to_competitor (bool), contact_email, contact_twitter, outreach_angle, domain_type; then GPT-4o-mini strict-JSON scores and ranks prospects by priority_score (0-100) and outputs priority_label (High/Medium/Low) + why. `GET /api/link-prospector/runs` (last 20, tenant-scoped). `GET /api/link-prospector/runs/:id`. `DELETE /api/link-prospector/runs/:id`. Table `link_prospector_runs` (id, tenant_id NOT NULL, keyword, domain, prospects JSONB, total_found, created_at).
- **Data source**: DataForSEO SERP (`/v3/serp/google/organic/live/regular`, location_code 2840 US) for live SERP pages; Perplexity Sonar for batch enrichment; GPT-4o-mini strict-JSON for scoring + ranking.
- **Template fallback**: 10 deterministic high-quality SEO authority prospects (moz.com, ahrefs.com, etc.) generated when DataForSEO is not configured — `source:'template'` honesty tag.
- **Priority scoring**: domain_strength (40%) + content_relevance (35%) + competitor_links bonus (10%) + domain_type bonus (edu/gov/news).
- **UI**: `/#link-prospector` under Reach → "8 · Backlinks" nav group. Keyword + domain inputs, Find button, strength + relevance filters. Prospects table: rank, page title (linked), domain, strength pill, relevance bar, competitor-link badge, contact email/Twitter, priority pill (score + label), "✉️ Draft" button (navigates to Cold Email Writer with domain and context pre-filled). Run history with load/delete. Backlink Intel view gains "🎯 Find Link Prospects" shortcut button (pre-fills domain from blTarget input and navigates to Link Prospector).

## Tier 45 — Influencer Fake-Detection & Deep Vetting

*   **T45 Influencer Fake-Detection & Deep Vetting** (Reach → Influencer CRM → 🔍 Vet) · `services/influencers/vetter.js` · `POST /api/influencers/:id/vet` — fetches influencer row by `(id, tenant_id)`, calls Perplexity Sonar to research the creator across follower growth trajectory, engagement quality, comment authenticity, brand partnerships, and engagement-pod signals; then GPT-4o-mini strict-JSON produces `{score 0-100, risk_level Low|Medium|High|Suspicious, red_flags[], green_flags[], summary, engagement_verdict, follower_quality}`. Results upserted onto `influencers` table (`vet_score`, `vet_risk`, `vet_flags JSONB`, `vetted_at`). Schema migration via `ALTER TABLE ... ADD COLUMN IF NOT EXISTS`. Template fallback (engagement-rate vs platform average heuristic) when API keys absent — `source:'template'`. Engagement rate benchmarks: Instagram 3%, TikTok 5%, YouTube 2%, X 0.5%, LinkedIn 1.5%.
- **Frontend**: Each CRM table row gains a "🔍 Vet" action button. Vetted rows show a colour-coded risk badge (green=Low, amber=Medium, red=High, purple=Suspicious) with score/100 in the Creator cell. Detail modal gains "🔍 Vet" / "🔄 Re-vet" button in header. Vet panel (score badge + risk pill + summary + red-flag/green-flag two-column chips) renders inside the detail modal when vet data exists. Toast shows live progress + final verdict. On completion, detail modal refreshes in place and table list reloads.
- **Scoring guide**: 80-100 = Low (authentic); 60-79 = Medium (mostly authentic, monitor); 40-59 = High (significant red flags); 0-39 = Suspicious (strong fraud signals).

## Tier 46 — Deleted Review Detection

*   **T46 Deleted Review Detection** (Compete → Review Aggregator → 🗑️ Deleted tab) · `services/review_monitor/{schema,api}.js` · table `review_snapshots` (id, tenant_id NOT NULL, brand, platform, review_id TEXT [SHA-256 of author|rating|body[:150]], rating, author, title, body, first_seen_at, last_seen_at, deleted_at, deleted BOOLEAN DEFAULT false) with UNIQUE(tenant_id, brand, platform, review_id) + two indexes (active brand/platform lookup + deleted_at DESC partial index).
- **Core helper** `recordSnapshot(tenantId, brand, platform, reviews[])`: called fire-and-forget from `services/review_aggregator/api.js` POST `/scan` after every successful Perplexity scan. Upserts current reviews (sets `deleted=false, deleted_at=null` if previously removed — resurrection), then bulk-marks any row for the same (tenant, brand, platform) that is absent from the current batch as `deleted=true, deleted_at=now()`. Returns newly-deleted rows.
- **Slack alert**: when deletions are found, fires `SLACK_WEBHOOK_URL` with a `🗑️ Review Deletion Alert` message listing brand, platform, count, and up to 3 review excerpts. SSRF-guarded (https-only URL validation). No-op when `SLACK_WEBHOOK_URL` unset.
- **Routes**: `GET /api/review-monitor/deleted?brand=&platform=` (last 200 deleted, tenant-scoped, optional brand+platform filters) · `GET /api/review-monitor/stats?brand=` (per-platform active_count, deleted_count, deleted_this_month + totalDeletedThisMonth rollup).
- **Frontend**: third tab `🗑️ Deleted` (with red badge counter for this-month deletions) added to `buildReviewAggregator()` alongside Single Brand and Compare tabs. Tab click triggers `window._raLoadDeleted()` which: (a) pre-fills brand/platform from the current form inputs; (b) parallel-fetches `/deleted` + `/stats`; (c) renders a per-platform grouped timeline of disappeared reviews showing author, star rating, title, body excerpt, first-seen date, disappeared date, and an "⚠️ Report to platform ↗" link; (d) renders a stats summary row of pills (per-platform deleted_count + active_count tracked); (e) updates the badge on the tab button.

## Tier 47 — WordPress Auto-Publishing

*   **T47 WordPress Publishing** (Create → 🌐 WordPress) · `services/wordpress/{schema,api}.js` · `GET /api/wordpress/sites`, `POST /api/wordpress/connect {name, site_url, username, app_password}`, `DELETE /api/wordpress/sites/:id`, `POST /api/wordpress/sites/:id/test`, `POST /api/wordpress/publish {site_id, title, content, status, excerpt}`, `GET /api/wordpress/publish-log` — stores per-tenant WordPress site connections (URL + username + Application Password, AES-256-GCM encrypted). Validates credentials via `GET /wp-json/wp/v2/users/me` on connect. Publishes via `POST /wp-json/wp/v2/posts`. SSRF-guarded (rejects private IPs). Publish log in `wordpress_publish_log`.
- **Tables**: `wordpress_sites (tenant_id, name, site_url, username, app_password[encrypted], status, last_tested_at)` · `wordpress_publish_log (tenant_id, site_id, wp_post_id, title, status, post_url, source)`
- **UI**: Settings panel `_initWordpressSettingsCard()` — add/test/remove sites inline. "📤 Publish to WP" button on every Content Calendar post card and Content Modes article. Publish modal: title, site selector, status (draft/publish/pending). Auto-injected via `ig_content_pro.js`.

## Tier 48 — Content Modes (Specialised Article Generator)

*   **T48 Content Modes** (Create → ✍️ Content Modes) · `services/content_modes/{schema,api}.js` · `POST /api/content-modes/generate {mode, keyword, brand, audience, tone, location?, existing_content?}`, `GET /api/content-modes/history`, `GET /api/content-modes/:id` — generates full SEO articles in 6 specialised modes. Strict-JSON GPT-4o-mini with mode-specific prompts. Template fallback when OpenAI key absent.
- **Modes**: `article` (standard SEO blog), `affiliation` (product review + pros/cons + verdict + rating), `ecommerce` (product/category pages + features/benefits/buying-guide), `local` (location-based + local keywords + GMB signals), `update` (refresh existing content — accepts paste), `discovery` (Google Discover-optimised, 500-700 words, curiosity hook).
- **Output fields**: title, seo_title, meta_description, word_count, intro, sections[], conclusion, cta, internal_link_suggestions[]. Affiliation adds: pros[], cons[], rating, verdict. E-commerce adds: features[], benefits[], buying_guide. Local adds: local_keywords[], gmb_signals[]. Discovery adds: hook, discovery_tips[].
- **Table**: `content_mode_runs (tenant_id, mode, keyword, brand, location, result JSONB, source)`
- **UI**: `/#content-modes` — mode selector (6 buttons), keyword/brand/audience/tone inputs, mode-specific extra fields (location for local, existing_content textarea for update). Article rendered with sections, pros/cons for affiliation, local keywords pills. 🎧 Audio Summary + 📤 Publish to WP + 📋 Copy buttons. Run history with load.

## Tier 49 — Content Autopilot

*   **T49 Content Autopilot** (Create → 🤖 Content Autopilot) · `services/autopilot/{schema,api}.js` · `POST /api/autopilot/schedules`, `GET /api/autopilot/schedules`, `PATCH /api/autopilot/schedules/:id`, `DELETE /api/autopilot/schedules/:id`, `POST /api/autopilot/run-now/:id`, `GET /api/autopilot/logs` — hourly cron checks `next_run_at <= now()`, calls Content Modes API internally, optionally auto-publishes to WordPress. Logs each run to `autopilot_logs`.
- **Frequencies**: daily · every3days · weekly · biweekly · monthly — `next_run_at` advances by the corresponding days after each run.
- **Tables**: `autopilot_schedules (tenant_id, name, brand, keyword, mode, channels, frequency, wp_site_id, auto_publish, wp_status, enabled, last_run_at, next_run_at, run_count)` · `autopilot_logs (schedule_id, tenant_id, status, detail, wp_post_url)`
- **Cron**: `startAutopilotCron()` called in BOOT_TASKS. First tick 30s after boot, then every 60 minutes. Each due schedule calls Content Modes `/generate` via internal HTTP with `INFOGENIE_API_KEY` auth, then optionally `POST /wp-json/wp/v2/posts`.
- **UI**: `/#content-autopilot` — schedule list (mode badge, frequency, WP site, pause/resume/delete/run-now), create-schedule sidebar (name, keyword, mode, frequency, WP site + status), recent runs log with status/detail/WP post link.

## Tier 50 — Audio Summaries

*   **T50 Audio Summaries** (Cross-feature helper) · `services/audio_summary/api.js` · `POST /api/audio-summary/generate {text, title, voice?}` — GPT-4o-mini condenses article to 250-300 word spoken prose (hook + 2-3 key points + takeaway), then OpenAI TTS (`tts-1`, default voice `nova`) generates MP3. Saves to `uploads/voiceovers/` and persists in `voiceover_runs` (reuses existing table). 6 voices: alloy, echo, fable, onyx, nova, shimmer.
- **UI**: Shared `window._audioSummary(text, title, btn?, outputContainerId?)` helper in `ig_content_pro.js`. Opens voice-picker modal → generates audio → renders inline `<audio>` player with ⬇ Download MP3. Called from: Content Modes article view (🎧 Audio Summary button), Content Calendar post cards (🎧 Audio button per card). Script viewable in `<details>` accordion.

## Tier 51 — AI Ad Creative Generator

*   **T51 Ad Creative Generator** (Create → 🎨 Ad Creative Gen) · `services/ad_creative/{schema,api}.js` · `POST /api/ad-creative/generate {platform, format, style, headline, body_copy, brand_name, brand_colors, cta_text, extra_context}`, `GET /api/ad-creative/history`, `GET /api/ad-creative/:id`, `DELETE /api/ad-creative/:id` — generates actual ad images via DALL-E 3 (OpenAI Images API). Saves to `uploads/ad_creatives/` and persists in `ad_creatives` table. Falls back to placeholder image when OpenAI key absent.
- **Platforms**: facebook_ad, instagram_post, instagram_story, google_display, twitter_x, linkedin, tiktok, pinterest, email_banner, youtube_thumbnail
- **Formats**: square (1024×1024), landscape (1792×1024), portrait (1024×1792) — maps directly to DALL-E 3 size param
- **Styles**: photorealistic, illustration, minimalist, bold_graphic, corporate, playful, editorial, flat_design
- **Table**: `ad_creatives (tenant_id, platform, format, style, headline, body_copy, brand_name, brand_colors, cta_text, image_url, image_path, prompt, source)`
- **UI**: `/#ad-creative` — platform/format/style selectors, headline/body/brand/CTA/colors inputs, generates image with download + copy URL + regenerate. History gallery grid with delete.

## Tier 52 — URL Brand Scanner

*   **T52 URL Brand Scanner** (Manage → Brand Foundation) · `POST /api/brand-foundation/scan-url {url}` — scrapes any public URL (Firecrawl if key present, raw HTTPS fetch fallback), strips HTML, passes page content to GPT-4o to extract brand identity fields (name, purpose, ICP, voice, positioning, brand_colors). SSRF-guarded (blocks localhost, private ranges). Returns `extracted` object for user review before applying.
- **UI**: `🔍 Scan from URL` bar injected above the Brand Foundation form by `_injectBrandScanner()` in `ig_creative_suite.js`. MutationObserver re-injects whenever Brand Foundation re-renders. Shows extraction preview cards, then one-click "✅ Apply to Brand Foundation" auto-saves via `/api/brand-foundation/save`.

## Tier 53/54 — Content Angles Library + Daily Idea Swipe Feed

*   **T53/T54 Idea Feed** (Create → 💡 Idea Swipe Feed) · `services/idea_feed/{schema,api}.js` · `GET /api/idea-feed/ideas?brand=&force=`, `POST /api/idea-feed/save/:id`, `POST /api/idea-feed/dismiss/:id`, `GET /api/idea-feed/saved`, `GET /api/idea-feed/angles` — generates 20 AI content ideas per day using brand context from Brand Foundation. Cached by `batch_date`; `force=1` refreshes. 20 content angles as static library.
- **20 Angles**: Mythbuster, Before & After, Us vs Them, Problem-Solution, Statistics, Negative Hook, What's Inside, Top Reasons, FAQ, Testimonial Story, Best-seller Spotlight, Behind the Scenes, Media Feature, Customer Mistake, Quick Win Tip, Trend Reaction, Founder Story, Product Demo, Social Proof, Limited Offer
- **Table**: `idea_feed_items (tenant_id, angle, headline, copy, channel, hashtags JSONB, cta, visual_concept, status, batch_date)`
- **UI**: `/#idea-feed` — Tinder-style card swipe (✕ Skip / ⭐ Save / 📅 Add to Calendar), angles library sidebar with one-click filter, saved ideas view. "Add to Calendar" calls `/api/content-calendar/add` and shows toast.

## Tier 55 — Multi-language Generation

*   **T55 Multi-language** (Cross-feature) — `language` param added to `POST /api/content-modes/generate` and `POST /api/content-calendar/generate`. 30 languages supported. When language ≠ English, a `Write ALL content in [language]` instruction is injected into every AI prompt. Language selector dropdown injected into Content Calendar and Content Modes forms via MutationObserver in `ig_creative_suite.js`. Supported: English, Spanish, French, German, Portuguese, Italian, Dutch, Polish, Russian, Turkish, Arabic, Japanese, Korean, Chinese (Simplified/Traditional), Hindi, Indonesian, Thai, Vietnamese, Swedish, Norwegian, Danish, Finnish, Czech, Romanian, Hungarian, Greek, Hebrew, Ukrainian, Malay.

## Tier 56 — AI Pitch Deck Builder
*   **T56 Pitch Deck Builder** (Create → 📊 Pitch Deck Builder) · `services/pitch_deck/{schema,api}.js` · `POST /api/pitch-deck/generate {brand, product, goal, audience, extra, num_slides}`, `POST /api/pitch-deck/export-pptx {id}`, `POST /api/pitch-deck/from-storyboard {frames}`, `GET /api/pitch-deck/history`, `GET /api/pitch-deck/:id` — GPT-4o generates a fully-structured pitch deck (investor/sales/marketing/product/internal) as JSON slide objects. `_buildPptx()` converts to PPTX via pptxgenjs v4: title slide, content slides with speaker notes, bullet points, stat callouts, visual direction placeholders. PPTX downloaded directly from browser. `/from-storyboard` converts Creator Studio storyboard frames to PPTX. Template fallback when AI unavailable. Frontend: `public/js/ig_design_studio.js` `buildPitchDeck()`.

## Tier 57 — WCAG Accessibility Audit
*   **T57 Accessibility Audit** (Analyse → ♿ Accessibility Audit) · `services/accessibility/{schema,api}.js` · `POST /api/accessibility/audit {url}`, `GET /api/accessibility/history`, `GET /api/accessibility/:id` — Fetches any public URL (Firecrawl → raw HTTPS fallback), runs `_basicHtmlChecks()` (structural: missing alt, labels, lang, headings, links) then GPT-4o deep WCAG 2.1 AA audit covering 12 criteria categories (images, color contrast, keyboard, forms, structure, links, ARIA, media, language, skip nav, empty elements). Returns scored 0-100 report with severity breakdown (critical/serious/moderate/minor), per-criterion fixes, passes list, and quick wins. SSRF-guarded. Template fallback when AI unavailable. Frontend: `buildAccessibility()` in ig_design_studio.js.

## Tier 58 — AI Wireframe Generator
*   **T58 Wireframe Generator** (Create → 📐 Wireframe Generator) · `services/wireframe/{schema,api}.js` · `POST /api/wireframe/generate {page_type, description, brand, sections, mobile}`, `GET /api/wireframe/history`, `GET /api/wireframe/:id` — 10 page types (landing/pricing/about/product/checkout/dashboard/blog/contact/login/portfolio). Claude (Anthropic) preferred for HTML generation; OpenAI fallback. Returns complete self-contained HTML wireframe with inline styles, gray placeholder boxes, realistic labels, sticky nav, CTA buttons. Rendered in srcdoc iframe in browser. Download as HTML or open in new tab. Template fallback when AI unavailable. Frontend: `buildWireframe()` in ig_design_studio.js.

## Tier 59 — Video → Slides (Storyboard PPTX Export)
*   **T59 Video-to-Slides** (Creator Studio → storyboard result) · Shares endpoint with T56: `POST /api/pitch-deck/from-storyboard {frames, brand, brand_color}` — converts Creator Studio storyboard frames (title/headline/voiceover/visual/bullets) directly into a PPTX pitch deck. Exposed via `window._exportStoryboardAsPptx(frames, brand, brandColor)` global helper in ig_design_studio.js for injection into the Creator Studio storyboard result panel.

## Tier 60 — AI Persona Studio
*   **T60 AI Persona Studio** (Create → 🎭 AI Persona Studio) · `services/persona/{schema,api}.js` · `GET /api/personas`, `POST /api/personas`, `GET /api/personas/:id`, `PUT /api/personas/:id`, `DELETE /api/personas/:id`, `POST /api/personas/:id/generate-avatar`, `POST /api/personas/:id/generate-content`, `POST /api/personas/ai-build` — Design and manage virtual AI influencer personas: name, niche, age range, gender, appearance prompt (for DALL-E 3), personality, content voice, posting style. AI Build mode auto-generates a full character from just a niche + audience description. Generate avatar images via DALL-E 3. Generate platform-specific content (Instagram caption, TikTok, Twitter thread, YouTube, LinkedIn) in the persona's voice with hashtags + CTA. Sample content saved to DB. Table `ai_personas`. Frontend: `buildPersonaStudio()` in ig_creator_suite.js.

## Tier 61 — iROAS Incrementality Module
*   **T61 iROAS Incrementality** (Grow → 📊 iROAS Incrementality) · `services/iroas/{schema,api}.js` · `GET /api/iroas/tests`, `POST /api/iroas/tests`, `PUT /api/iroas/tests/:id`, `DELETE /api/iroas/tests/:id`, `POST /api/iroas/tests/:id/interpret`, `POST /api/iroas/saturation` — Measure true incremental ROAS from holdout tests. Design a holdout experiment (channel, holdout %, date range), enter raw results (test/control reach + conversions, ad spend, reported revenue, AOV), and get auto-calculated: test CVR, control CVR, lift %, incremental conversions, incremental revenue, iROAS, reported ROAS, and delta between them. AI interpretation (GPT-4o) gives a plain-English verdict (positive/neutral/negative), grade, and recommended actions. Budget saturation curves (GPT-4o) show spend vs iROAS per channel with the saturation point and diminishing returns insight. Tables `iroas_tests`, `iroas_saturation`. Frontend: `buildIroas()` in ig_creator_suite.js.

## Tier 62 — AI Marketing Agent (Goal Loops)
*   **T62 AI Marketing Agent** (Manage → 🤖 AI Marketing Agent) · `services/agent_goals/{schema,api}.js` · `GET /api/agent-goals`, `POST /api/agent-goals`, `GET /api/agent-goals/:id`, `PUT /api/agent-goals/:id`, `DELETE /api/agent-goals/:id`, `POST /api/agent-goals/:id/evaluate`, `POST /api/agent-goals/:id/tasks`, `PUT /api/agent-goals/tasks/:taskId` — Set a marketing goal with success criteria and deadline. GPT-4o generates a 5-8 task execution plan with action types (content_creation/campaign_launch/audience_build/seo/outreach/analysis/competitor_research), priorities, and due dates. Tasks rendered as a checklist; checking a task auto-updates goal progress %. "Evaluate Progress" runs a judge LLM that grades the goal (A-F), assesses whether it's on track, identifies the priority next action, and suggests adjustments. Tables `agent_goals`, `agent_tasks`. Frontend: `buildAgentGoals()` in ig_creator_suite.js.

## Tier 63 — E-commerce Product Video
*   **T63 E-commerce Product Video** (Create → 🛒 Product Video) · `POST /api/ecom-video/generate {product_name, product_description, target_audience, platform, video_style, persona_name}` — Inline route in server.js (no schema — storyboard is ephemeral). GPT-4o generates a 6-scene product video storyboard optimised for conversion: hook, scene-by-scene breakdown (shot type, action, voiceover, text overlay, duration), CTA, ready-to-post caption, and hashtags. Platform options: Instagram Reels/TikTok, YouTube Shorts, Facebook Video Ad, Pinterest Video. Style options: lifestyle+demo, UGC authentic review, before-and-after, unboxing, product showcase. Optionally links to a saved AI Persona (T60) as the presenter. Template fallback when AI unavailable. Frontend: `buildEcomVideo()` in ig_creator_suite.js.

## Tier 64 — Brand Deal Pipeline
*   **T64 Brand Deal Pipeline** (Manage → 🤝 Brand Deal Pipeline) · `services/brand_deals/{schema,api}.js` · `GET /api/brand-deals`, `POST /api/brand-deals`, `PUT /api/brand-deals/:id`, `DELETE /api/brand-deals/:id`, `POST /api/brand-deals/:id/generate-pitch`, `GET /api/brand-deals/stats/summary` — Kanban pipeline for influencer brand deals across 7 statuses: inquiry → negotiating → accepted → active → completed → rejected → paused. Track brand, contact, product, deal type (sponsored_post/affiliate/gifted/ambassador/product_review/ugc/other), offered rate, negotiated rate, currency, deliverables, follow-up date, deadline, linked AI Persona (T60). AI pitch generator (GPT-4o) writes initial pitch, follow-up, or counter-offer emails with subject, body, tone, and key points. Stats summary: total inquiries, active deals, completed, total earned, pipeline value. Table `brand_deals` (FK to `ai_personas`). Frontend: `buildBrandDeals()` in ig_creator_suite.js.

## Tier 65 — Organic Social Monitor (Apify · TikTok)
*   **T65 Organic Social Monitor** (Compete → 📱 Organic Social Monitor) · `services/apify/api.js` · `POST /api/apify/tiktok-organic {keyword, limit}` — Triggers Apify's `clockworks~tiktok-scraper` Actor to search TikTok organic posts for any brand or keyword. Returns views, likes, comments, shares, and engagement rate per video plus aggregate summary (total views, avg likes). Results rendered as card grid with per-video ER colour-coded; CSV export. Frontend: `buildOrganic()` in `public/js/ig_apify.js`. Shares the `GET /api/apify/run-status` polling endpoint with T66.

## Tier 66 — Local Lead Finder (Apify · Google Maps)
*   **T66 Local Lead Finder** (Reach → 📍 Local Lead Finder) · `services/apify/api.js` · `POST /api/apify/maps-leads {query, limit}` — Triggers Apify's `compass~crawler-google-places` Actor to scrape local businesses from Google Maps. Returns name, category, address, phone, website, star rating, review count, opening hours. Results rendered as card grid with summary stats (% with phone, % with website). CSV export + one-click HubSpot push via `/api/hubspot/create-company`. Both features require `APIFY_API_KEY` env var — show a self-serve sign-up banner when missing. Frontend: `buildLocalLeads()` in `public/js/ig_apify.js`.

## Tier 67 — Audience Research Agent
*   **T67 Audience Research Agent** (Grow → Landing Pages → Research Audience) · `services/landing_pages/api.js` · `POST /api/landing-pages/research-audience {product, market, goal}` — AI-powered pre-page audience intelligence. Returns 3-4 audience segments (persona name, demographics, search trigger), 5-7 specific pain points, 4-5 objections with rebuttals, 8-12 power words, and 5 headline hook angles. Results rendered inline above the LP form; "Apply to Brief" auto-fills the audience and brief fields, pre-loading research into the generation prompt. GPT-4o-mini; template fallback. No DB persistence (ephemeral research step). Frontend: `_lpRunResearch()` + `_lpApplyResearch()` in app.js.

## Tier 68 — Pre-launch Creative Scoring
*   **T68 Pre-launch Creative Scoring** (Create → Ad Creative → 🎯 Score panel) · `services/ad_creative/api.js` · `POST /api/ad-creative/score {headline, body_copy, cta_text, platform, visual_desc}` — Score any ad creative before spending money. Returns 5 dimension scores (hook_strength, cta_clarity, urgency, emotional_resonance, relevance, each 1-10), overall score (0-100), letter grade (A-F), predicted CTR range (low/high %), a plain-English verdict, and 3-5 specific actionable improvement tips per weak dimension. Powered by GPT-4o-mini; template fallback scores. Rendered as a bar-chart scorecard with grade badge. Frontend: `window._csScore()` + Creative Scorer panel appended to `buildAdCreative()` in ig_creative_suite.js.

## Tier 69 — UGC Video Ad Scripts
*   **T69 UGC Video Ad Scripts** (Create → Ad Creative → 🎬 UGC panel) · `services/ad_creative/api.js` · `POST /api/ad-creative/ugc-script {product, audience, benefit, hook_type, platform, duration}` — Generate a complete timed UGC video script (15/30/45/60s) optimised for Instagram Reels, TikTok, YouTube Shorts, or Facebook Reels. Returns scene-by-scene breakdown with timestamp, type badge (hook/problem/solution/proof/cta), exact spoken words, camera/action direction, and on-screen text overlays. Also returns full post caption and creator filming tips. 5 hook styles: problem-solution, before-after, testimonial, ASMR, trending-sound. GPT-4o-mini; template fallback. Frontend: `window._ugcGenerate()` + UGC Script panel appended to `buildAdCreative()` in ig_creative_suite.js.

## Tier 70 — Live A/B Split Testing
*   **T70 Live A/B Split Testing** (Grow → Landing Pages → 🧪 A/B tab) · `services/landing_pages/api.js` · `POST /api/landing-pages/ab-variant {page_id, angle}`, `GET /api/landing-pages/ab-results/:pageId`, `POST /api/landing-pages/ab-view/:pageId {variant}`, `POST /api/landing-pages/ab-convert/:pageId {variant}` — Generate an alternative landing page variant with a different persuasion angle (social-proof-heavy, fear-based, curiosity, authority, benefit-led, urgency, storytelling). Variant B stored in `landing_page_ab_variants` table (tenant-scoped). Live pages served at `/lp/:id` (public) split 50/50 by cookie `lp_v_{id}`. View and conversion events tracked per variant. Dashboard "A/B Test" tab shows side-by-side iframe previews and real-time stats (views, leads, CVR). Public bypass for view/convert endpoints. Tables: `landing_page_ab_variants`, `landing_pages` (views, conversions columns). Frontend: `_lpGenABVariant()`, `_lpLoadABResults()`, `_lpDownloadVariant()` in app.js.

## Tier 71 — Lead Analytics + Webhook
*   **T71 Lead Analytics + Webhook** (Grow → Landing Pages → 📋 Leads tab) · `services/landing_pages/api.js` · `POST /api/landing-pages/lead/:pageId` (public), `GET /api/landing-pages/leads/:pageId`, `PUT /api/landing-pages/webhook/:pageId {webhook_url}` — Lead capture form auto-injected into every generated landing page HTML and served at `/lp/:id`. Submissions stored in `landing_page_leads` (tenant_id, page_id, name, email, message, variant). Webhook forwarding on each new lead (HTTP POST to any URL — Zapier, HubSpot, Make, etc.). Dashboard Leads tab shows total/today/variant-B stats, a paginated leads table, and a webhook URL input with save button. Lead endpoint is public (reads tenant_id from LP row). Conversion counter auto-incremented on each lead. Tables: `landing_page_leads`, `landing_pages` (webhook_url, conversions columns). Frontend: `_lpLoadLeads()`, `_lpSetWebhook()` in app.js.

## Tier 72 — Create Once Publish Everywhere
*   **T72 Create Once Publish Everywhere** (Grow → Landing Pages → 📣 Ad Package tab) · `services/ad_creative/api.js` · `POST /api/ad-creative/from-landing-page {page_id}` — Generate a complete 3-platform ad package from any saved landing page. Pulls headline, subhead, CTA, social proof, and features from the LP's stored JSONB content. Returns tailored copy for: Meta (FB+IG) — headline ≤30 chars, 125-char primary text, CTA button, format/size notes; Google Display — responsive headline, 90-char description, CTA; TikTok — 3s hook script, 2-3 sentence video concept, hashtags, creator direction. Each package card has a one-click Copy button. GPT-4o-mini; template fallback using LP headline directly. Requires page saved to DB. Frontend: `_lpGenAdPackage()` in app.js; 📣 Ad Package tab in `buildLandingPages` result area.

## Tier 73 — Competitor Change Monitor
*   **T73 Change Monitor** (Compete → 🔔 Change Monitor) · `services/change_monitor/{schema,api}.js` · `GET /api/change-monitor/watches`, `POST /watches`, `DELETE /watches/:id`, `POST /watches/:id/pause`, `POST /check/:id`, `GET /diffs/:watchId` — Watch any competitor URL for content changes. On manual or scheduled check: fetches page via Firecrawl, computes word-level delta (%, set diff). If delta ≥5%, runs GPT-4o-mini analysis to classify change type (pricing/features/messaging/design/content/other), severity (low/medium/high), and produce a strategic insight. Changes stored in `change_monitor_diffs`; full snapshot (up to 50k chars) in `change_monitor_watches.last_snapshot`. Alerts: email + Slack flags. Tables: `change_monitor_watches`, `change_monitor_diffs` (both tenant-scoped). Frontend: `buildChangeMonitor()` in `public/js/ig_intelligence_tools.js`.

## Tier 74 — Conversion Recovery Assistant
*   **T74 Conversion Recovery** (Grow → 🔄 Conversion Recovery) · `services/conversion_recovery/api.js` · `POST /api/conversion-recovery/score {platform, pixel_type, monthly_ad_spend, browser_targeting}`, `POST /setup-guide {platform, goal, business_platform, server_lang}` — Two-panel tool. Score Calculator: computes a 0-100 Recovery Score based on iOS/Safari loss, ad blockers, cross-device, cookie consent, and late pixel fires. Returns breakdown by loss category, total recoverable %, and estimated monthly dollar gap. Setup Guide Generator: GPT-4o-mini generates a complete step-by-step server-side tracking setup (Meta CAPI or Google Enhanced Conversions) with copy-paste code snippets for Node/Python/PHP. Template fallback for common scenarios. No DB. Frontend: `buildConversionRecovery()` in `public/js/ig_intelligence_tools.js`.

## Tier 75 — AI Web Extractor
*   **T75 AI Web Extractor** (Compete → 🧲 AI Web Extractor) · `services/web_extractor/api.js` · `POST /api/web-extractor/extract {url, instruction}` — Free-form data extraction in plain English. SSRF guard (dns.promises lookup blocks private IPs). Fetches page via Firecrawl markdown, then prompts GPT-4o-mini with the extraction instruction to produce structured JSON (columns + rows). Returns table with summary and row count. Example prompts built into UI. No DB. Frontend: `buildWebExtractor()` in `public/js/ig_intelligence_tools.js`.

## Tier 76 — Scraping Recipe Library
*   **T76 Recipe Scraper** (Compete → 📋 Scraping Recipe Library) · `services/recipe_scraper/api.js` · `GET /api/recipe-scraper/recipes`, `POST /run {recipe_id, target_url}` — 10 pre-built extraction recipes: G2 reviews, Product Hunt, Trustpilot, LinkedIn company, HN discussion, App Store, Glassdoor, Capterra, competitor pricing page, jobs page. Each recipe defines extracted fields and a precise extraction instruction. Fetches via Firecrawl + GPT-4o-mini extraction. Returns columns/rows/summary. No DB. Frontend: `buildRecipeScraper()` in `public/js/ig_intelligence_tools.js`.

## Tier 77 — Multi-Source Lead Aggregator
*   **T77 Lead Aggregator** (Reach → 🌐 Multi-Source Lead Sweep) · `services/lead_aggregator/api.js` · `GET /api/lead-aggregator/sources`, `POST /sweep {industry, location, role, company_size, sources}` — Sweeps leads from Perplexity (AI web research via sonar model) and Apollo.io simultaneously. Deduplicates by email/LinkedIn URL/name+company key. Scores leads (+15 for email, +10 for LinkedIn, +5 for domain, +10 for title match). Returns sorted leads with source_counts breakdown. Source availability shown live from API key check. No DB. Frontend: `buildLeadAggregator()` in `public/js/ig_intelligence_tools.js`.

## Tier 78 — Dataset Marketplace
*   **T78 Dataset Market** (Compete → 📦 Dataset Marketplace) · `services/dataset_market/api.js` · `GET /api/dataset-market/packs`, `POST /generate {pack_id, industry, region, competitors}` — 8 intelligence dataset packs: competitor pricing, job market signals, social proof aggregator, industry benchmarks, tech stack map, content gaps, ad creative intelligence, market sizing. Each generates a structured table (≥8 rows) via GPT-4o with key_takeaways and AI disclaimer. Template fallback when AI unavailable. No DB. Frontend: `buildDatasetMarket()` in `public/js/ig_intelligence_tools.js`.

## Tier 79 — AI Model Comparison
*   **T79 Model Compare** (Manage → ⚖️ Model Comparison) · `services/model_compare/api.js` · `GET /api/model-compare/models`, `POST /run {prompt, task_type, models, max_tokens, system_prompt}` — Run any prompt on up to 5 models simultaneously: GPT-4o, GPT-4o-mini (OpenAI), Claude 3.5 Sonnet, Claude 3 Haiku (Anthropic), Gemini 1.5 Flash, Gemini 1.5 Pro (Google), Llama 3.1 8B (Cloudflare). Parallel API calls with per-model latency and token tracking. AI judge (GPT-4o-mini) picks winner and scores each model on quality/creativity/accuracy/conciseness. Model availability driven by configured API keys. No DB. Frontend: `buildModelCompare()` in `public/js/ig_intelligence_tools.js`.

## Tier 80 — Resilient Competitor Tracker
*   **T80 Resilient Tracker** (Compete → 🛡️ Resilient Tracker) · `services/resilient_tracker/{schema,api}.js` · `GET /api/resilient-tracker/trackers`, `POST /trackers`, `DELETE /trackers/:id`, `POST /check/:id`, `GET /snapshots/:trackerId` — Track specific data points (key|description pairs) on any competitor page. Check cascade: (1) Firecrawl fetch → GPT-4o-mini extract → if primary fails, (2) Perplexity sonar fallback. Data stored per tracker with `last_data` JSONB, method used, consecutive_failures counter. Each check creates a snapshot in `resilient_tracker_snapshots`. History modal shows all snapshots with values and method. Tables: `resilient_trackers`, `resilient_tracker_snapshots` (both tenant-scoped). Frontend: `buildResilientTracker()` in `public/js/ig_intelligence_tools.js`.

## Tier 81 — AI Competitor War Room
*   **T81 AI Competitor War Room** (Analyse → ⚔️ AI Competitor War Room) · `services/war_room/{schema,api}.js` · `POST /api/war-room/analyse {competitor, domain}`, `GET /api/war-room/history` — Cross-signal AI prediction engine. Gathers live signals via Perplexity sonar (recent hires, ad spend changes, new pages, pricing changes, PR, geographic expansion) then feeds all signals into GPT-4o to produce a strategic move prediction with confidence score (0–100), move type (market_expansion/product_launch/pricing_war/talent_surge/acquisition/channel_shift/partnership), threat level (low/medium/high/critical), rationale, recommended counter-move, and per-signal weight breakdown. Template fallback when AI unavailable. Table: `war_room_analyses` (tenant-scoped). Frontend: `buildWarRoom()` in `public/js/ig_advanced_features.js`.

## Tier 82 — AI Revenue Forecast Engine
*   **T82 Revenue Forecast Engine** (Grow → 💹 AI Revenue Forecast Engine) · `services/revenue_forecast/{schema,api}.js` · `POST /api/revenue-forecast/simulate {scenario_name, current_monthly_spend, budget_change, current_leads, conversion_rate, aov, cac, ltv, currency}`, `GET /api/revenue-forecast/history` — 90-day what-if revenue modeller. GPT-4o produces best/expected/worst case scenarios (leads, customers, revenue, CAC, ROAS), payback period, LTV/CAC ratio, plain-English recommendation, key assumptions, risk factors, and 3-month breakdown table. Template fallback with linear extrapolation when AI unavailable. Table: `revenue_forecasts` (tenant-scoped). Frontend: `buildRevenueforecast()` in `public/js/ig_advanced_features.js`.

## Tier 83 — Business Digital Twin
*   **T83 Digital Twin** (Grow → 🪞 Digital Twin) · `services/digital_twin/{schema,api}.js` · `GET /api/digital-twin/scenarios`, `POST /api/digital-twin/simulate {scenario, question, business_context}`, `GET /api/digital-twin/history` — AI business simulation engine. 7 preset scenarios (increase spend, launch UK, cut Meta, double down on SEO, raise prices, launch product, custom) plus free-form "what if" questions. GPT-4o returns verdict (positive/neutral/negative/mixed), confidence, executive summary, 90-day timeline (3 phases), upsides, risks, metrics affected with directional estimates, recommended action, and alternative scenarios to explore. Template fallback. Table: `digital_twin_scenarios` (tenant-scoped). Frontend: `buildDigitaltwin()` in `public/js/ig_advanced_features.js`.

## Tier 84 — AI Acquisition Engine
*   **T84 Acquisition Engine** (Reach → 🚀 AI Acquisition Engine) · `services/acquisition_engine/{schema,api}.js` · `GET /api/acquisition-engine/campaigns`, `POST /launch {name, target_industry, target_role, target_size, value_prop, count}`, `POST /prospects/:id/mark-sent`, `POST /prospects/:id/mark-meeting` — Fully autonomous lead-to-meeting pipeline. Finds prospects via Perplexity sonar, scores each (email +40, company +20, role +20, base +20), generates a 3-step cold email sequence via GPT-4o, and tracks the pipeline from found → emailed → meeting_booked. Tables: `acquisition_campaigns` + `acquisition_prospects` (both tenant-scoped). Frontend: `buildAcquisitionengine()` in `public/js/ig_advanced_features.js`.

## Tier 85 — Investor Mode
*   **T85 Investor Mode** (Manage → 📈 Investor Mode) · `services/investor_mode/{schema,api}.js` · `POST /api/investor-mode/generate {company_name, stage, mrr, arr, growth_rate, cac, ltv, burn_rate, runway_months, paying_customers, currency, highlights, challenges}`, `GET /api/investor-mode/history`, `GET /api/investor-mode/portal/:token` (public), `GET /investor/:token` (public HTML portal) — AI CFO report generator. GPT-4o produces executive summary, 8-metric dashboard (MRR/ARR/growth/CAC/LTV/LTV:CAC/runway/customers each with trend direction), 12-month revenue forecast, highlights, challenges, the ask, investor narrative, and next milestones. Generates a unique `portal_token` → public HTML read-only investor portal at `/investor/:token` (no auth). Table: `investor_reports` (tenant-scoped). Frontend: `buildInvestormode()` in `public/js/ig_advanced_features.js`.

## Tier 86 — AI Business Acquisition Scanner
*   **T86 Business Acquisition Scanner** (Analyse → 🔭 Business Acquisition Scanner) · `services/biz_scanner/{schema,api}.js` · `GET /api/biz-scanner/types`, `POST /api/biz-scanner/scan {industry, region, scan_type, budget_range}`, `GET /api/biz-scanner/history` — M&A opportunity intelligence. Scan types: struggling competitors, businesses for sale, fast-growing sectors, franchise opportunities, or full scan. Perplexity sonar gathers live market data then GPT-4o scores each opportunity (1–100) on strategic value, price attractiveness, distress signals, and growth trajectory. Returns market summary, scored opportunity cards with why-interesting + next action + signals, top pick, and sector trends. Table: `biz_scan_runs` (tenant-scoped). Frontend: `buildBizscanner()` in `public/js/ig_advanced_features.js`.

## Tier 87 — AI Marketing Marketplace
*   **T87 Marketplace** (Manage → 🛍️ AI Marketing Marketplace) · `services/marketplace/{schema,api}.js` · `GET /api/marketplace/listings`, `GET /api/marketplace/my-listings`, `POST /api/marketplace/listings`, `PUT /api/marketplace/listings/:id`, `DELETE /api/marketplace/listings/:id`, `POST /api/marketplace/listings/:id/download`, `GET /api/marketplace/stats` — Multi-tenant marketplace for sharing marketing assets. 8 categories: template, campaign, audience, prompt_pack, landing_page, email_sequence, ai_agent, creative. Browse public listings with search + category filter. Publish any asset as a listing with tags and structured content. Download records a counter + a `marketplace_downloads` row. Tenant owners can update/delete their own listings. Stats API returns total listings, my listings count, and total downloads from my listings. Tables: `marketplace_listings` + `marketplace_downloads` (tenant-scoped). Frontend: `buildMarketplace()` in `public/js/ig_advanced_features.js`.

## Tier 88 — Autonomous Marketing Operator
*   **T88 Auto Operator** (Grow → 🦾 Autonomous Marketing Operator) · `services/auto_operator/{schema,api}.js` · `GET /api/auto-operator/runs`, `POST /api/auto-operator/run {trigger, current_roas, target_roas, current_cac, campaigns, issues}` — The "I already fixed it" AI. GPT-4o acts as an autonomous operator: diagnoses the problem, takes 3+ decisive actions (rewrite_ad/pause_campaign/scale_campaign/reallocate_budget/launch_ab_test/update_landing_page/send_alert/adjust_bid), produces rewritten ad copy, budget reallocation plan, A/B test proposal, and a one-sentence summary of what was done and what to expect. All actions logged. Table: `auto_operator_runs` (tenant-scoped). Template fallback when AI unavailable. Frontend: `buildAutooperator()` in `public/js/ig_advanced_features.js`.

## Tier 89 — Benchmark Intelligence
*   **T89 Benchmark Intelligence** (Analyse → 📊 Benchmark Intelligence) · `services/benchmarks/{schema,api}.js` · `GET /api/benchmarks/config`, `POST /api/benchmarks/submit {vertical, region, company_size, metrics}`, `POST /api/benchmarks/compare {vertical, region, company_size, your_metrics}`, `GET /api/benchmarks/leaderboard` — Cross-customer anonymised benchmark data moat. Tenants submit their own metrics (CPA, CAC, ROAS, CTR, CVR, CPM, email open rate, LTV, LTV:CAC, ad spend, organic traffic) which are aggregated via PERCENTILE_CONT into P25/median/P75 per vertical × region × company size. Compare view shows your metrics vs network medians, GPT-4o identifies above/below/on-par status with gap %, takeaway, and action for each metric, plus an overall summary and "biggest opportunity". Tables: `benchmark_submissions` + `benchmark_aggregates` (both tenant-scoped). Frontend: `buildBenchmarks()` in `public/js/ig_moat_features.js`.

## Tier 90 — Media Mix Modeler
*   **T90 Media Mix Modeler** (Grow → 🎛️ Media Mix Modeler) · `services/mmm/{schema,api}.js` · `POST /api/mmm/model {scenario_name, total_budget, current_channels, goals, timeframe_weeks, industry, target_audience}`, `GET /api/mmm/history` — Forward-looking media allocation engine (MMM). GPT-4o models saturation curves (linear/log/s-curve), diminishing returns, lag effects, attribution windows, and cross-channel synergy for each channel to recommend the optimal budget split. Returns per-channel budget + %, expected revenue/conversions, recommended budget shifts with rationale, channel health (under/optimal/over-saturated), and three scenario projections (conservative/base/aggressive with total revenue + ROAS). Uplift vs current allocation calculated. Table: `mmm_models` (tenant-scoped). Frontend: `buildMmm()` in `public/js/ig_moat_features.js`.

## Tier 91 — Brand Safety & Compliance Engine
*   **T91 Brand Safety** (Manage → 🔒 Brand Safety & Compliance) · `services/brand_safety/{schema,api}.js` · `GET /api/brand-safety/rules`, `POST /api/brand-safety/check {check_type, content, vertical, jurisdictions, url}`, `GET /api/brand-safety/history` — Compliance guardrail layer for ad copy, landing pages, emails, social posts. Six jurisdiction rule-sets: FTC (US), GDPR (EU), FCA (UK Finance), ESMA (EU Finance), HIPAA (US Health), ASA (UK General). GPT-4o audits against selected ruleset and returns: overall_verdict (pass/caution/fail), risk score 0–100, per-flag (rule, jurisdiction, severity info/warning/critical, quote, issue, fix), compliant points, required disclaimers, and a full compliant rewrite. Table: `brand_safety_checks` (tenant-scoped). Frontend: `buildBrandSafety()` in `public/js/ig_moat_features.js`.

## Tier 92 — Safe Autonomous Agent
*   **T92 Safe Agent** (Grow → 🛡️ Safe Agent) · `services/safe_agent/{schema,api}.js` · `POST /api/safe-agent/propose {objective, context, budget_guardrail}`, `POST /api/safe-agent/approve/:id`, `POST /api/safe-agent/reject/:id`, `POST /api/safe-agent/rollback/:id`, `GET /api/safe-agent/proposals`, `GET /api/safe-agent/audit-log/:id` — Trustworthy autonomous execution pipeline: propose → simulate → approve → execute → rollback. GPT-4o produces a structured action plan (per-step: action, channel, detail, estimated cost, reversible flag) plus pre-execution simulation (expected outcome, confidence %, best/worst case, revenue impact, ROAS change, risk factors). Safety checks listed (pass/warn/fail). Budget guardrail enforced at proposal time. Full audit log every state transition. One-click rollback on executed proposals. Tables: `safe_agent_proposals` + `safe_agent_audit_log` (tenant-scoped). Frontend: `buildSafeAgent()` in `public/js/ig_moat_features.js`.

## Tier 93 — Data Provenance Layer
*   **T93 Data Provenance** (Manage → 🔍 Data Provenance) · `services/data_provenance/{schema,api}.js` · `POST /api/data-provenance/log`, `POST /api/data-provenance/bulk-log`, `GET /api/data-provenance/insights`, `GET /api/data-provenance/insights/:key` — First-class data audit trail. Every insight logged with: source_type (api/scrape/ai_generated/user_input/database/third_party), source_name, source_url, is_ai_estimated flag, ai_model, freshness_hours, and fetched_at timestamp. Filter by source type, AI/real status, or search. Summary stats: total tracked, real vs AI-estimated count, AI ratio gauge, breakdown by source type. Freshness colour-coded (green < 24h, amber < 7d, red older). Table: `insight_provenance` (tenant-scoped, indexed on tenant_id + insight_key). Frontend: `buildDataProvenance()` in `public/js/ig_moat_features.js`.

## Tier 95 — Unified Decision Engine
*   **T95 Decision Engine** (Manage → 8 · Intelligence & governance → 🧭 Decision Engine) · `services/decision_engine/{schema,api}.js` · `POST /api/decision-engine/analyse {context_notes}`, `GET /api/decision-engine/recommendations`, `POST /api/decision-engine/act/:id`, `POST /api/decision-engine/dismiss/:id` — Cross-platform ranked recommendation engine. Pulls live signal counts from optimizer, SEO, SERP tables then calls GPT-4o to synthesise 8 prioritised recommendations across 7 categories (budget, channel, creative, audience, seo, lifecycle, competitive). Each recommendation includes: expected_impact, confidence_pct (0–100), cost_estimate, time_to_result, priority_score (0–100), data_sources. Saved to `decision_recommendations` (tenant-scoped) with acted_at / dismissed_at workflow. Template fallback when AI unavailable. Frontend: `buildDecisionEngine()` in `public/js/ig_strategic_features.js`.

## Tier 96 — Experiment Suite
*   **T96 Experiment Suite** (Grow → 6 · Test & measure incrementally → 🧬 Experiment Suite) · `services/experiment_suite/{schema,api}.js` · `GET /api/experiments/list`, `POST /api/experiments/create {name, type, hypothesis, control_group, variant_group, channels, budget_split, start_date}`, `PUT /api/experiments/:id/update {status, outcome, lift_pct, confidence_pct, p_value, institutional_learning}`, `POST /api/experiments/:id/analyse` — Incrementality experiment tracking with 5 types: a_b, geo_lift, holdout, creative_incrementality, audience_saturation. Status lifecycle: draft → running → paused → complete / cancelled. AI analysis endpoint (GPT-4o-mini) interprets results: verdict (significant/not_significant/inconclusive), practical vs statistical significance, next action, institutional learning. Table: `experiments` (tenant-scoped). Frontend: `buildExperimentSuite()` in `public/js/ig_strategic_features.js`.

## Tier 97 — Identity Spine (CDP-lite)
*   **T97 Identity Spine** (Reach → 6 · First-party data spine → 🪪 Identity Spine) · `services/identity_spine/{schema,api}.js` · `GET /api/identity/stats`, `GET /api/identity/profiles?stage=`, `POST /api/identity/import {contacts[]}`, `POST /api/identity/score {profile_ids?}`, `POST /api/identity/event {profile_id, event_type, channel, properties}` — Consent-aware unified customer profile store. Batch CSV import (up to 500 contacts). AI scoring (GPT-4o-mini) assigns ltv_score ($0–$10k estimated LTV), propensity_score (0–100 purchase likelihood %), lifecycle_stage, and next_best_action per profile. Stage filter: unknown/aware/interested/considering/customer/churned. Event logging updates last_seen_at. Tables: `identity_profiles` + `identity_events` (both tenant-scoped). Frontend: `buildIdentitySpine()` in `public/js/ig_strategic_features.js`.

## Tier 98 — Approval Workflows
*   **T98 Approval Workflows** (Manage → 8 · Intelligence & governance → ✅ Approval Workflows) · `services/approval_workflows/{schema,api}.js` · `GET /api/approvals/policies`, `POST /api/approvals/policies {name, action_types, budget_threshold, notify_email}`, `DELETE /api/approvals/policies/:id`, `GET /api/approvals/queue`, `POST /api/approvals/request {action_type, title, description, budget_impact, channel}`, `POST /api/approvals/approve/:id`, `POST /api/approvals/reject/:id`, `POST /api/approvals/execute/:id`, `POST /api/approvals/rollback/:id {reason}` — Human-in-the-loop governance for AI-proposed actions. 8 action types: pause_campaign, scale_budget, launch_campaign, send_email, publish_content, audience_change, price_change, other. Pre-execution simulation (GPT-4o-mini) produces expected_outcome, confidence %, best/worst case, risk_factors, reversible flag, simulation_verdict (safe/caution/high_risk). Full state machine: pending → approved/rejected → executed → rolled_back. Policy engine defines which action types require approval by budget threshold. Tables: `approval_policies` + `approval_requests` (tenant-scoped). Frontend: `buildApprovalWorkflows()` in `public/js/ig_strategic_features.js`.

## Tier 99 — AI Answer Share-of-Voice
*   **T99 AI Answer SOV** (Analyse → 2 · Know your competition → 🧠 AI Answer SOV) · `services/ai_answer_sov/{schema,api}.js` · `GET /api/ai-answer-sov/questions`, `POST /api/ai-answer-sov/questions/add {questions[], topic_cluster, brand_domain}`, `DELETE /api/ai-answer-sov/questions/:id`, `POST /api/ai-answer-sov/sweep {question_ids?, providers?, brand_domain}`, `GET /api/ai-answer-sov/dashboard` — Citation-tracking for AI engines. Tracks whether LLMs (GPT-4o-mini, Claude Haiku, Perplexity sonar) mention your brand when answering buyer questions, and which competitors get cited. Sweep runs each active question across selected providers; extracts brand_mentioned (boolean), brand_sentiment (positive/neutral/not_mentioned), competitor domain citations via URL regex. Dashboard: brand SOV %, brand_hits/total responses, by-provider breakdown, top competing domains ranked by citation frequency, and recent sweep feed. Tables: `sov_questions` + `sov_responses` (both tenant-scoped). Frontend: `buildAiAnswerSov()` in `public/js/ig_strategic_features.js`.

## Tier 100 — Revenue Intelligence
*   **T100 Revenue Intelligence** (Manage → 8 · Intelligence & governance → 💼 Revenue Intelligence) · `services/revenue_intel/{schema,api}.js` · `GET /api/revenue-intel/config`, `GET /api/revenue-intel/accounts?stage=`, `POST /api/revenue-intel/accounts/add {company_name, domain, industry, pipeline_stage, pipeline_value, owner_name}`, `POST /api/revenue-intel/signal {account_id, signal_type, source, description}`, `POST /api/revenue-intel/score-account {account_id}`, `GET /api/revenue-intel/triggers`, `GET /api/revenue-intel/pipeline` — B2B account intent scoring and pipeline influence. 7 pipeline stages: awareness→interest→intent→consideration→decision→customer→churned. 10 signal types with score weights (pricing_page=15, demo_request=25, offline_meeting=20, page_visit=2, etc.) accumulate into intent_score (0–100). AI scoring (GPT-4o-mini) returns recommended_play, outreach_angle, urgency, deal_probability_pct, buying_committee_size, recommended_actions. Hot triggers endpoint: accounts with intent ≥ 50 and signals in past 7 days. Pipeline view: value by stage with waterfall bars. Tables: `revenue_accounts` + `revenue_signals` (tenant-scoped). Frontend: `buildRevenueIntel()` in `public/js/ig_strategic_features.js`.

## Tier 101 — Proprietary Data Products
*   **T101 Data Products** (Analyse → 2 · Know your competition → 📦 Proprietary Data Products) · `services/data_products/{schema,api}.js` · `GET /api/data-products/config`, `POST /api/data-products/creative/submit {format, hook_type, cta_type, channel, ctr, cvr, roas, industry}`, `GET /api/data-products/creative/index?channel=&industry=`, `POST /api/data-products/offer/submit {offer_type, pricing_model, free_trial, trial_days, guarantee, cvr, industry}`, `GET /api/data-products/offer/benchmarks`, `GET /api/data-products/emerging-signals`, `POST /api/data-products/emerging-signals/refresh {industry, focus}` — Three-product proprietary data suite. (1) Creative Performance Index: tenants submit creative results (8 formats × 8 hook types × 8 CTA types) aggregated via AVG into benchmark tables by format/hook/CTA; (2) Offer Benchmark Network: pricing model × offer type CVR comparisons, free-trial lift analysis; (3) Emerging Market Signals: GPT-4o generates 8 typed signals (competitor_move, pricing_shift, hiring_intent, content_theme, channel_trend, etc.) with confidence scores and category tags. Tables: `creative_performance` + `offer_benchmark_data` + `emerging_signals` (all tenant-scoped). Frontend: `buildDataProducts()` in `public/js/ig_strategic_features.js`.

## Tier 94 — Vertical Playbooks
*   **T94 Vertical Playbooks** (Manage → 📋 Vertical Playbooks) · `services/vertical_playbooks/{schema,api}.js` · `GET /api/playbooks/list`, `GET /api/playbooks/:vertical`, `POST /api/playbooks/activate/:id`, `GET /api/playbooks/active/list`, `POST /api/playbooks/generate-custom {industry, business_description, goals, challenges, budget}` — Pre-built, benchmarked 90-day industry growth packs. Six system playbooks on first request: E-Commerce (Google Shopping + Meta DPA + email flows), SaaS (LinkedIn + intent search + PLG), Local Business (Maps + reviews + community), Finance (FCA/FTC compliant demand gen), Health (evidence-based FTC-safe content), Agency (case study engine + client referral). Each includes: channels list, compliance requirements, benchmark targets, and 3-phase 90-day roadmap with weekly task lists. `generate-custom` uses GPT-4o to create a bespoke playbook for any industry. Activate a playbook to track it in the tenant's active list. Tables: `vertical_playbooks` (system + custom) + `active_playbooks` (tenant-scoped). Frontend: `buildVerticalPlaybooks()` in `public/js/ig_moat_features.js`.

## Tier 102 — Multi-Agent Swarm
*   **T102 Multi-Agent Swarm** (Manage → 8 · Intelligence & governance → 🐝 Multi-Agent Swarm) · `services/agent_swarm/{schema,api}.js` · `GET /api/swarm/config`, `GET /api/swarm/configs`, `POST /api/swarm/configs {name, trigger_event, agent_chain, description}`, `PUT /api/swarm/configs/:id`, `DELETE /api/swarm/configs/:id`, `POST /api/swarm/trigger {event_type, event_data, config_id?}`, `GET /api/swarm/runs`, `GET /api/swarm/runs/:id` — Inter-agent orchestration engine. Trigger events: competitor_change, ad_rejected, roas_drop, budget_pacing, keyword_spike, content_published, lead_scored, manual. Agent types: competitor_spy, media_buyer, copywriter, seo_analyst, email_composer, audience_builder, approval_gate, slack_notify, webhook. Each trigger fires a configurable chain of GPT-4o-mini agents that communicate sequentially — each receives prior steps' outputs as context. Full run history with step-level logs. Tables: `swarm_configs` + `swarm_runs` + `swarm_steps` (tenant-scoped). Frontend: `buildAgentSwarm()` in `public/js/ig_agentic_suite.js`.

## Tier 103 — Self-Healing Ad Accounts
*   **T103 Self-Healing Ad Accounts** (Grow → 7 · Autonomous execution → 🩹 Self-Healing Ad Accounts) · `services/self_healing/{schema,api}.js` · `GET /api/self-healing/config`, `GET /api/self-healing/history`, `POST /api/self-healing/check {platform, ad_name, rejection_reason, original_copy}`, `POST /api/self-healing/heal/:id`, `POST /api/self-healing/resubmit/:id`, `POST /api/self-healing/resolve/:id` — AI detects ad rejections, rewrites copy to comply with platform policies (Meta/Google/TikTok/LinkedIn/Microsoft), and prepares healed copy for resubmission. GPT-4o analyses rejection reason + original copy against platform-specific policy rules, outputs healed_headline, healed_copy, heal_strategy, policy_fixes[], confidence_pct. Status lifecycle: detected → healing → resubmitted → resolved. Heal attempts logged. Tables: `ad_rejections` + `heal_attempts` (tenant-scoped). Frontend: `buildSelfHealing()` in `public/js/ig_agentic_suite.js`.

## Tier 104 — Cross-Platform Budget Arbitrage
*   **T104 Cross-Platform Budget Arbitrage** (Grow → 7 · Autonomous execution → ⚖️ Cross-Platform Budget Arbitrage) · `services/budget_arbitrage/{schema,api}.js` · `GET /api/budget-arbitrage/config`, `GET /api/budget-arbitrage/rules`, `POST /api/budget-arbitrage/rules {name, platforms, total_daily_budget, max_shift_pct, reallocation_logic}`, `DELETE /api/budget-arbitrage/rules/:id`, `POST /api/budget-arbitrage/analyse {platform_metrics[], total_daily_budget?, max_shift_pct?}`, `POST /api/budget-arbitrage/execute/:id`, `GET /api/budget-arbitrage/history` — Intraday predictive budget reallocation across platforms. GPT-4o analyses live ROAS/CPA/CVR/impression-share per platform and recommends specific dollar shifts (respecting max_shift_pct guardrail). Output: per-platform recommended_budget, shift_amount, shift_direction, reason, roas_efficiency rating, projected_roas_uplift_pct, urgency, risk_level. Execute marks a recommendation as applied. Logic types: roas_weighted, cpa_weighted, cvr_weighted, impression_share, equal_split. Tables: `budget_arb_rules` + `budget_arb_history` (tenant-scoped). Frontend: `buildBudgetArbitrage()` in `public/js/ig_agentic_suite.js`.

## Tier 105 — Server-Side Event Manager
*   **T105 Server-Side Event Manager** (Manage → 2 · Monitor performance → 📡 Server-Side Event Manager) · `services/ss_events/{schema,api}.js` · `GET /api/ss-events/config`, `GET /api/ss-events/sources`, `POST /api/ss-events/sources/add {source_name, source_type, pixel_id}`, `DELETE /api/ss-events/sources/:id`, `POST /api/ss-events/ingest/:token` (public — token-authenticated batch ingest), `POST /api/ss-events/ingest` (authenticated), `GET /api/ss-events/report?days=30` — Full server-side tracking architecture (TripleWhale / Northbeam pattern). Source types: web, mobile, offline, crm, phone, email, pos, api. Event types: purchase, lead, page_view, phone_call, offline_sale, form_submit, subscription, demo_request, etc. All PII hashed SHA-256 (email, phone, IP) before storage. Each source gets a unique ingest_token URL for pixel-free server-to-server event delivery. Report: totals, by_event_type, by_source, revenue timeline. Tables: `ss_sources` + `ss_events` (tenant-scoped). Frontend: `buildSsEvents()` in `public/js/ig_agentic_suite.js`.

## Tier 106 — CTV & Streaming Audio
*   **T106 CTV & Streaming Audio** (Grow → 8 · Next-gen ad channels → 📺 CTV & Streaming Audio) · `services/ctv/{schema,api}.js` · `GET /api/ctv/config`, `GET /api/ctv/campaigns`, `POST /api/ctv/campaigns/create {name, platform, objective, budget, daily_budget, targeting, start_date}`, `PUT /api/ctv/campaigns/:id/status`, `POST /api/ctv/campaigns/:id/ai-brief {brand_name, industry, target_audience, key_message}`, `GET /api/ctv/report` — Campaign management for Connected TV and streaming audio platforms: Roku OneView, Hulu Ad Manager, Amazon DSP (Fire TV), YouTube TV (DV360), Spotify Ads, Pandora/SiriusXM, iHeartRadio, Apple Podcast Ads. Creates campaigns with projected reach and CPM estimates. AI creative brief (GPT-4o) generates 30-second TV script (scene-by-scene), visual direction, voiceover, CTA, brand recall technique, or audio-specific 30-second script + host-read version. Tables: `ctv_campaigns` + `ctv_performance` (tenant-scoped). Frontend: `buildCtv()` in `public/js/ig_agentic_suite.js`.

## Tier 107 — RCS & Apple Messages
*   **T107 RCS & Apple Messages** (Reach → 8 · Next-gen messaging → 💬 RCS & Apple Messages) · `services/rcs/{schema,api}.js` · `GET /api/rcs/config`, `GET /api/rcs/campaigns`, `POST /api/rcs/campaigns/create {name, channel, message_body, rich_card, cta_buttons, sender_name}`, `POST /api/rcs/campaigns/0/ai-generate {channel, goal, brand_name, offer, cta_label, cta_url}`, `POST /api/rcs/campaigns/:id/send {recipients[]}`, `GET /api/rcs/stats/:id` — Rich Communication Services (Android) and Apple Messages for Business campaigns. AI generator (GPT-4o) produces: plain text fallback (max 160 chars), rich_card (title/description/layout/media_url), cta_buttons (open_url/dial_phone/view_location), suggested_replies, sender_name, best_send_time, personalisation_tips. Channels: rcs (Android RCS), apple_messages. Send pipeline queues up to 1,000 recipients per campaign with status tracking (queued → sent → delivered → read). Tables: `rcs_campaigns` + `rcs_messages` (tenant-scoped). Frontend: `buildRcs()` in `public/js/ig_agentic_suite.js`.

## Tier 108 — Auto-Submit LLM Knowledge Bases
*   **T108 LLM Knowledge Base** (Analyse → 2 · Know your competition → 🧬 LLM Knowledge Base) · `services/llm_kb/{schema,api}.js` · `GET /api/llm-kb/config`, `GET /api/llm-kb/entries`, `POST /api/llm-kb/entries/add {entry_type, title, content, seo_keywords}`, `DELETE /api/llm-kb/entries/:id`, `POST /api/llm-kb/generate {brand_name, brand_description, industry, target_audience, key_products, competitors}`, `GET /api/llm-kb/schema/:id` (generates JSON-LD + llms.txt + robots.txt instructions), `GET /api/llm-kb/llms-txt` (plain text download) — Auto-generates structured knowledge bases that maximise brand citation in ChatGPT, Claude, and Perplexity. Entry types: faq, how_to, about_brand, product_service, comparison, use_case, pricing, testimonial_proof. GPT-4o generates LLM-optimised content using citation-friendly patterns (authoritative language, specific numbers, Q&A format). Schema endpoint produces JSON-LD (FAQPage, HowTo, Article), llms.txt file, and step-by-step submission instructions (HTML embed, robots.txt allow rules for GPTBot/ClaudeBot/PerplexityBot). Tables: `llm_kb_entries` + `llm_kb_submissions` (tenant-scoped). Frontend: `buildLlmKb()` in `public/js/ig_agentic_suite.js`.

## Tier 109 — Global Privacy Auto-Compliance
*   **T109 Privacy Auto-Compliance** (Manage → 10 · Privacy & compliance → 🔐 Privacy Auto-Compliance) · `services/privacy_compliance/{schema,api}.js` · `GET /api/privacy-compliance/config`, `GET /api/privacy-compliance/history`, `POST /api/privacy-compliance/scrub {contacts[], jurisdictions[]}`, `POST /api/privacy-compliance/check-dnc {phone_numbers[]}`, `POST /api/privacy-compliance/validate-consent {contacts[], channels[], jurisdictions[]}`, `POST /api/privacy-compliance/audit {list_type, channels, jurisdictions}` — Operational compliance automation for GDPR, CCPA, CAN-SPAM, TCPA, AU Privacy Act, CA PIPEDA. PII scrubber detects SSN, credit card, passport, DOB, and IP patterns via regex. DNC checker flags toll-free/premium numbers (TCPA). Consent validator checks for explicit opt-in, consent_date, consent_source per channel per jurisdiction. Full AI compliance audit (GPT-4o) returns overall_risk, compliance_score (0–100), required_actions with jurisdiction + urgency, best_practices. All checks logged with risk_level. Tables: `privacy_checks` + `consent_records` (tenant-scoped). Frontend: `buildPrivacyCompliance()` in `public/js/ig_agentic_suite.js`.

## Tier 110 — Brand DNA & BYOM
*   **T110 Brand DNA / BYOM** (Manage → 3 · AI tools & config → 🧬 Brand DNA & Custom AI Training) · `services/brand_dna/{schema,api}.js` · `GET /api/brand-dna/config`, `GET /api/brand-dna/assets`, `POST /api/brand-dna/assets/add {asset_type, title, content, channel, performance_metric, performance_value}`, `DELETE /api/brand-dna/assets/:id`, `POST /api/brand-dna/profile/build`, `GET /api/brand-dna/profile`, `POST /api/brand-dna/generate {content_type, topic, length, variants}`, `POST /api/brand-dna/score {content}` — Custom brand voice training via retrieval-augmented generation. Asset types: winning_ad, top_email, brand_guideline, tone_sample, competitor_ref, landing_page, social_post, video_script. Profile/build analyses all tenant assets with GPT-4o to extract: brand_voice, tone_descriptors[], writing_style, banned_phrases[], sentence_length, example_headlines[], example_hooks[], example_ctas[], colour_of_language. Generate uses the profile as a strict prompt constraint to produce on-brand content (8 content types, 3 length options, up to 5 variants). Score grades any content A–F (0–100) against the profile, identifies banned phrases, suggests a rewrite. Tables: `brand_dna_assets` + `brand_dna_profiles` (tenant-scoped, profiles UNIQUE per tenant). Frontend: `buildBrandDna()` in `public/js/ig_agentic_suite.js`.

## Tier 111 — Visual No-Code Workflow Automation Builder
*   **T111 Workflow Automation Builder** (Manage → 11 · Automation studio → 🔧 Workflow Automation Builder) · `services/workflow_builder/{schema,api}.js` · `GET /api/workflow-builder/config`, `GET /api/workflow-builder/list`, `GET /api/workflow-builder/:id`, `POST /api/workflow-builder/create {name, trigger_type, trigger_config, nodes[]}`, `PUT /api/workflow-builder/:id`, `DELETE /api/workflow-builder/:id`, `POST /api/workflow-builder/:id/toggle`, `POST /api/workflow-builder/:id/run {trigger_data?}`, `GET /api/workflow-builder/runs/all`, `POST /api/workflow-builder/ai-build {description}`, `POST /api/workflow-builder/webhook/:token` — Visual no-code workflow builder (Make.com / n8n equivalent inside InfoGenie). Trigger types: manual, schedule, webhook, event_lead_created, event_deal_stage, event_ad_rejected, event_roas_drop, event_competitor_change. Node types: condition_if, delay_wait, action_send_email, action_post_slack, action_http_webhook, action_tag_contact, action_create_campaign, action_update_record, action_ai_generate, action_send_rcs, split_ab. AI builder (GPT-4o) converts natural-language descriptions into complete workflow JSON. Run engine executes nodes sequentially, logs step results, handles action_ai_generate (calls GPT-4o-mini) and action_http_webhook (real HTTP delivery with 10s timeout). Inbound webhook receiver for external triggers. Full run history with step logs. Tables: `wf_workflows` + `wf_runs` (tenant-scoped). Frontend: `buildWorkflowBuilder()` in `public/js/ig_agentic_suite.js`.

## Tier 112 — Google Trends Explorer
*   **T112 Google Trends** (SEO → 8 · Find search opportunity → 📈 Google Trends) · `services/google_trends/api.js` · `GET /api/google-trends/interest?keyword&period&geo`, `GET /api/google-trends/related?keyword&geo`, `GET /api/google-trends/trending?geo&count`, `GET /api/google-trends/compare?keywords&geo&period` — Free real-time keyword trend signals via the `google-trends-api` npm package (no API key required). Interest over time (0–100 normalised) for 1–3 keywords with period selector (24h → 5y) and region filter. Related queries shows top + rising queries with breakout badge. Trending now surfaces daily trending searches per country with traffic estimates and top news article. Compare mode plots up to 5 keywords side-by-side with per-keyword sparkline + avg/peak summary. 15-minute in-memory cache prevents hammering the unofficial endpoint. React component: `components/features/analyse/GoogleTrends.tsx`.

## Tier 113 — Bing Webmaster Tools
*   **T113 Bing Webmaster Tools** (SEO → 8 · Find search opportunity → 🔷 Bing Webmaster Tools) · `services/bing_webmaster/{schema,api}.js` · `GET /api/bing-webmaster/status`, `GET /api/bing-webmaster/sites`, `GET /api/bing-webmaster/activity?siteUrl&period`, `GET /api/bing-webmaster/page-stats?siteUrl&page`, `GET /api/bing-webmaster/keywords?siteUrl&q&country&language`, `GET /api/bing-webmaster/crawl?siteUrl`, `GET /api/bing-webmaster/history?snap_type` — Bing-specific keyword performance, page query stats, and site activity as a free second SEO data source alongside Google Search Console. Requires `BING_WEBMASTER_API_KEY` (set via Manage → 🔑 Platform APIs; obtain from bing.com/webmasters → Settings → API Access). Snapshots saved to `bing_wmt_snapshots` (tenant-scoped). Shows graceful setup prompt when key is not configured. Platform-key live test: `GetSites` probe returns 401/403 for bad key. React component: `components/features/analyse/BingWebmaster.tsx`.

## Tier 116 — Real-Time News
*   **T116 Real-Time News** (Monitor → 📰 Real-Time News) · `services/realtime_news/{schema,api}.js` · `GET /api/realtime-news/status`, `POST /api/realtime-news/search {query, country, lang, limit}`, `POST /api/realtime-news/topic {topic, country, lang, limit}`, `POST /api/realtime-news/headlines {topic, country, lang}`, `POST /api/realtime-news/saved`, `GET /api/realtime-news/saved`, `DELETE /api/realtime-news/saved/:id`, `GET /api/realtime-news/history` — Live news intelligence powered by the Real-Time News Data API on RapidAPI (RAPIDAPI_KEY, `real-time-news-data.p.rapidapi.com`). Three-tab interface: (1) **Search** — keyword/brand/competitor search with recent search history and 🔖 save; (2) **Topic Feeds** — curated live feeds across BUSINESS, TECHNOLOGY, WORLD, SCIENCE, HEALTH, ENTERTAINMENT with country filter; (3) **Saved Articles** — bookmarked article library persisted to `rtn_saved_articles`. All articles show source badge, relative publish time, and image thumbnails. Graceful "not subscribed" prompt links directly to the free-tier RapidAPI subscription page when the key is absent or not yet subscribed. Tables: `rtn_saved_articles` + `rtn_search_history` (tenant-scoped). React component: `components/features/monitor/RealtimeNews.tsx`.

## Tier 117 — Customer 360

*   **T117 Customer 360** (Manage → 2 · Monitor performance → 🧭 Customer 360) · `services/customer_360/api.js` · `GET /api/customer-360/overview` — Read-only, aggregation-only unified account dashboard. No new tables — stitches together existing tenant data at request time: `brand_foundation` (brand snapshot: name, industry, ICP, positioning), latest `voc_runs` (Voice of Customer theme summary: praise/complaint/question/feature_request counts + top themes), lead totals + recent activity feed merged from `landing_page_leads` + `linksell_leads` + KV-scoped `qualified_leads` (`_tkvScope.tkey('qualified_leads', tid)`), `audience_segments`, and `brand_deals`. Empty-state cards deep-link to the source tool (Brand Foundation, VoC Mining, Lead Finder, Dynamic Audiences, Brand Deals) when a section has no data yet. Tenant-scoped via `resolveTenantId`. React component: `components/features/manage/Customer360.tsx`.
