# xAI / Billy chat
XAI_API_KEY=
XAI_MODEL=grok-4.6
PORT=5000

# Temporary Bearer gate for POST /v1/billy/* only (not the /api/* session gate).
INFOGENIE_API_TOKEN=

# Existing /api/* session-or-key gate (Authorization: Bearer / X-InfoGenie-Key).
# INFOGENIE_API_KEY=
